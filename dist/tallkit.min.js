(function(t, e) {
	"object" == typeof exports && "undefined" != typeof module ? e(exports) : "function" == typeof define && define.amd ? define(["exports"], e) : e((t = "undefined" != typeof globalThis ? globalThis : t || self).TALLKit = {});
})(this, function(t) {
	Object.defineProperty(t, Symbol.toStringTag, { value: "Module" });
	var e = Object.defineProperty, i = (t, i) => {
		let s = {};
		for (var n in t) e(s, n, {
			get: t[n],
			enumerable: !0
		});
		return i || e(s, Symbol.toStringTag, { value: "Module" }), s;
	};
	function s(t) {
		const e = Number.parseFloat(t);
		return Number.isNaN(e) ? 0 : t.trim().endsWith("ms") ? e : 1e3 * e;
	}
	function n(t) {
		const e = window.getComputedStyle(t), i = e.transitionDuration.split(","), n = e.transitionDelay.split(",");
		return i.reduce((t, e, i) => {
			const r = n[i] ?? n[n.length - 1] ?? "0s";
			return Math.max(t, s(e) + s(r));
		}, 0);
	}
	function r(t, e = {}) {
		let i = null, s = null, r = !1;
		const o = () => {
			null !== i && (clearTimeout(i), i = null), s && (t.removeEventListener("transitionend", s), s = null);
		}, a = () => {
			r || (r = !0, o(), e.remove && t.isConnected && t.remove(), e.onDone?.());
		}, l = window.matchMedia("(prefers-reduced-motion: reduce)").matches, h = (e = [], i = []) => {
			e.length && t.classList.remove(...e), i.length && t.classList.add(...i);
		};
		if (l) return h(e.from, e.to), e.start?.(), e.finish?.(), a(), () => {};
		h(e.to, e.from), e.start?.(), requestAnimationFrame(() => {
			t.offsetHeight, h(e.from, e.to), e.finish?.();
		}), s = (e) => {
			e.target === t && a();
		}, t.addEventListener("transitionend", s);
		const c = n(t);
		if (0 === c) a();
		else {
			const t = Math.max(.1 * c, 50);
			i = window.setTimeout(a, c + t);
		}
		return o;
	}
	function o(t, e = {}) {
		return r(t, {
			from: ["opacity-100"],
			to: ["opacity-0"],
			remove: !0,
			...e
		});
	}
	function a(t, e = {}) {
		const i = window.getComputedStyle(t), s = t.offsetHeight, n = i.marginTop, o = i.marginBottom, a = i.paddingTop, l = i.paddingBottom;
		return t.style.height = `${s}px`, t.style.overflow = "hidden", t.style.marginTop = n, t.style.marginBottom = o, t.style.paddingTop = a, t.style.paddingBottom = l, t.style.opacity = "1", t.offsetHeight, r(t, {
			...e,
			start() {
				t.style.willChange = "height, margin, padding, opacity", e.start?.();
			},
			finish() {
				t.style.height = "0px", t.style.marginTop = "0px", t.style.marginBottom = "0px", t.style.paddingTop = "0px", t.style.paddingBottom = "0px", t.style.opacity = "0", e.finish?.();
			},
			onDone() {
				t.style.removeProperty("height"), t.style.removeProperty("overflow"), t.style.removeProperty("margin-top"), t.style.removeProperty("margin-bottom"), t.style.removeProperty("padding-top"), t.style.removeProperty("padding-bottom"), t.style.removeProperty("opacity"), t.style.removeProperty("will-change"), e.onDone?.();
			}
		});
	}
	var l = /* @__PURE__ */ new Map();
	async function h(t, { integrity: e, crossorigin: i } = {}) {
		if (Array.isArray(t)) return t.reduce((t, s) => t.then(async (t) => [...t, await h(s, {
			integrity: e,
			crossorigin: i
		})]), Promise.resolve([]));
		if (l.has(t)) return l.get(t);
		const s = new Promise((s, n) => {
			if (document.querySelector(`script[src="${t}"]`)) return void s(new Event("load"));
			const r = document.createElement("script");
			r.src = t, r.defer = !0, e && (r.integrity = e), (e || i) && (r.crossOrigin = i ?? "anonymous"), r.onload = s, r.onerror = (e) => {
				l.delete(t), n(e);
			}, document.head.appendChild(r);
		});
		return l.set(t, s), s;
	}
	async function c(t, e, i) {
		t() || (await h(e), i && await m(i));
	}
	var u = /* @__PURE__ */ new Map();
	async function d(t) {
		if (Array.isArray(t)) return Promise.all(t.map((t) => d(t)));
		if (u.has(t)) return u.get(t);
		const e = import(t).catch((e) => {
			throw u.delete(t), e;
		});
		return u.set(t, e), e;
	}
	var p = /* @__PURE__ */ new Map();
	function m(t, { integrity: e, crossorigin: i } = {}) {
		if (Array.isArray(t)) return t.reduce((t, s) => t.then(async (t) => [...t, await m(s, {
			integrity: e,
			crossorigin: i
		})]), Promise.resolve([]));
		if (p.has(t)) return p.get(t);
		const s = new Promise((s, n) => {
			if (document.querySelector(`link[rel="stylesheet"][href="${t}"]`)) return void s(new Event("load"));
			const r = document.createElement("link");
			r.rel = "stylesheet", r.href = t, e && (r.integrity = e), (e || i) && (r.crossOrigin = i ?? "anonymous"), r.onload = s, r.onerror = (e) => {
				p.delete(t), n(e);
			}, document.head.appendChild(r);
		});
		return p.set(t, s), s;
	}
	function g(t, e) {
		const i = t instanceof Element ? [t] : t;
		Array.from(i ?? []).filter((t) => t instanceof Element).forEach((t, i) => {
			window.Alpine.bind(t, "function" == typeof e ? e(t, i) : e);
		});
	}
	function f(t, e, i) {
		g(t, { [`@keydown.${e}.document`](t) {
			t.preventDefault(), i(t);
		} });
	}
	var v = /^#([0-9a-f]{3}|[0-9a-f]{4}|[0-9a-f]{6}|[0-9a-f]{8})$/i, y = /^rgba?\(\s*(\d{1,3})\s*,\s*(\d{1,3})\s*,\s*(\d{1,3})\s*(?:,\s*([\d.]+%?)\s*)?\)$/i, w = /^hsla?\(\s*([\d.]+)\s*,\s*([\d.]+)%\s*,\s*([\d.]+)%\s*(?:,\s*([\d.]+%?)\s*)?\)$/i;
	function b(t) {
		return Math.max(0, Math.min(255, Math.round(Number(t))));
	}
	function k(t) {
		return Math.max(0, Math.min(1, Number.isFinite(t) ? t : 1));
	}
	function A(t) {
		return parseFloat(k(t).toFixed(2));
	}
	function $(t) {
		return void 0 === t ? 1 : k(t.endsWith("%") ? parseFloat(t) / 100 : parseFloat(t));
	}
	function x(t, e, i) {
		t /= 255, e /= 255, i /= 255;
		const s = Math.max(t, e, i), n = Math.min(t, e, i), r = (s + n) / 2, o = s - n;
		let a = 0, l = 0;
		if (0 !== o) {
			switch (l = o / (1 - Math.abs(2 * r - 1)), s) {
				case t:
					a = (e - i) / o % 6;
					break;
				case e:
					a = (i - t) / o + 2;
					break;
				default: a = (t - e) / o + 4;
			}
			a = Math.round(60 * a), a < 0 && (a += 360);
		}
		return [
			a,
			Math.round(100 * l),
			Math.round(100 * r)
		];
	}
	function E({ r: t, g: e, b: i, a: s }, n) {
		const r = [
			t,
			e,
			i
		].map((t) => b(t).toString(16).padStart(2, "0")).join("");
		return n ? `#${r}${Math.round(255 * k(s)).toString(16).padStart(2, "0")}` : `#${r}`;
	}
	function C(t) {
		if ("string" != typeof t) return null;
		const e = t.trim();
		if (!e) return null;
		let i;
		if (i = e.match(v)) return function(t) {
			const e = 3 === t.length || 4 === t.length, i = e ? t[0] + t[0] : t.slice(0, 2), s = e ? t[1] + t[1] : t.slice(2, 4), n = e ? t[2] + t[2] : t.slice(4, 6), r = e ? 4 === t.length ? t[3] + t[3] : null : 8 === t.length ? t.slice(6, 8) : null;
			return {
				r: parseInt(i, 16),
				g: parseInt(s, 16),
				b: parseInt(n, 16),
				a: null === r ? 1 : parseInt(r, 16) / 255
			};
		}(i[1].toLowerCase());
		if (i = e.match(y)) return {
			r: b(i[1]),
			g: b(i[2]),
			b: b(i[3]),
			a: $(i[4])
		};
		if (i = e.match(w)) {
			const { r: t, g: e, b: s } = function(t, e, i) {
				t = (t % 360 + 360) % 360, e = k(e / 100), i = k(i / 100);
				const s = (1 - Math.abs(2 * i - 1)) * e, n = s * (1 - Math.abs(t / 60 % 2 - 1)), r = i - s / 2, [o, a, l] = t < 60 ? [
					s,
					n,
					0
				] : t < 120 ? [
					n,
					s,
					0
				] : t < 180 ? [
					0,
					s,
					n
				] : t < 240 ? [
					0,
					n,
					s
				] : t < 300 ? [
					n,
					0,
					s
				] : [
					s,
					0,
					n
				];
				return {
					r: Math.round(255 * (o + r)),
					g: Math.round(255 * (a + r)),
					b: Math.round(255 * (l + r))
				};
			}(parseFloat(i[1]), parseFloat(i[2]), parseFloat(i[3]));
			return {
				r: t,
				g: e,
				b: s,
				a: $(i[4])
			};
		}
		return null;
	}
	function S(t, e = "hex") {
		const i = C(t);
		return i ? function({ r: t, g: e, b: i, a: s = 1 }, n = "hex") {
			const r = k(s);
			switch (n) {
				case "hexa": return E({
					r: t,
					g: e,
					b: i,
					a: r
				}, !0);
				case "rgb": return `rgb(${b(t)}, ${b(e)}, ${b(i)})`;
				case "rgba": return `rgba(${b(t)}, ${b(e)}, ${b(i)}, ${A(r)})`;
				case "hsl": {
					const [s, n, r] = x(t, e, i);
					return `hsl(${s}, ${n}%, ${r}%)`;
				}
				case "hsla": {
					const [s, n, o] = x(t, e, i);
					return `hsla(${s}, ${n}%, ${o}%, ${A(r)})`;
				}
				default: return E({
					r: t,
					g: e,
					b: i
				}, !1);
			}
		}(i, e) : null;
	}
	function D(t) {
		return String(t).padStart(2, "0");
	}
	function M(t) {
		return `${t.getFullYear()}-${D(t.getMonth() + 1)}-${D(t.getDate())}`;
	}
	function T(t) {
		if (!t) return null;
		const [e, i, s] = t.split("-").map(Number);
		return new Date(e, i - 1, s);
	}
	function F(t) {
		return new Date(t.getFullYear(), t.getMonth(), 1);
	}
	function I(t, e) {
		return new Date(t.getFullYear(), t.getMonth() + e, 1);
	}
	function _(t) {
		return new Date(t.getFullYear(), t.getMonth() + 1, 0);
	}
	function L(t, e = 0) {
		const i = (t.getDay() - e + 7) % 7;
		return B(M(t), -i);
	}
	function B(t, e) {
		const i = T(t);
		return i.setDate(i.getDate() + e), M(i);
	}
	function O(t, e) {
		return t.getFullYear() === e.getFullYear() && t.getMonth() === e.getMonth();
	}
	function j(t, e) {
		return Math.round((T(e) - T(t)) / 864e5);
	}
	function z(t) {
		const e = new Date(Date.UTC(t.getFullYear(), t.getMonth(), t.getDate())), i = e.getUTCDay() || 7;
		e.setUTCDate(e.getUTCDate() + 4 - i);
		const s = new Date(Date.UTC(e.getUTCFullYear(), 0, 1));
		return Math.ceil(((e - s) / 864e5 + 1) / 7);
	}
	function q(t) {
		try {
			const e = new Intl.DateTimeFormat(t, {
				year: "numeric",
				month: "2-digit",
				day: "2-digit"
			}).formatToParts(new Date(2e3, 0, 2)).filter((t) => [
				"day",
				"month",
				"year"
			].includes(t.type)).map((t) => t.type);
			if (3 === e.length) return e;
		} catch {}
		return [
			"month",
			"day",
			"year"
		];
	}
	function N(t, e) {
		const i = T(t);
		return i ? new Intl.DateTimeFormat(e, {
			year: "numeric",
			month: "2-digit",
			day: "2-digit"
		}).format(i) : "";
	}
	function R(t, e) {
		if (!t) return null;
		const i = String(t).match(/\d+/g);
		if (!i || i.length < 3) return null;
		const s = q(e), n = {};
		if (s.forEach((t, e) => {
			n[t] = i[e];
		}), !n.day || !n.month || !n.year) return null;
		const r = Number(n.day), o = Number(n.month);
		let a = Number(n.year);
		2 === n.year.length && (a += a < 70 ? 2e3 : 1900);
		const l = new Date(a, o - 1, r);
		return l.getFullYear() !== a || l.getMonth() !== o - 1 || l.getDate() !== r ? null : M(l);
	}
	function P(t) {
		const [e, i] = t.split(":").map(Number);
		return 60 * e + i;
	}
	function U(t) {
		const e = /^(\d{1,2}):(\d{2})$/.exec(String(t).trim());
		if (!e) return null;
		const i = Number(e[1]), s = Number(e[2]);
		return i > 23 || s > 59 ? null : `${D(i)}:${D(s)}`;
	}
	function W(t = document.documentElement) {
		return "rtl" === t.dir || "rtl" === getComputedStyle(t).direction;
	}
	async function H(t, e = 2) {
		try {
			return await t();
		} catch (i) {
			if (e <= 0 || "AbortError" === i.name || "NotFoundError" === i.name) throw i;
			return H(t, e - 1);
		}
	}
	function V(t, e) {
		if (t.startsWith("image/")) return "image";
		if (t.startsWith("video/")) return "video";
		if (t.startsWith("audio/")) return "audio";
		switch (e.split(".").pop()?.toLowerCase() ?? "") {
			case "jpg":
			case "jpeg":
			case "png":
			case "gif": return "image";
			case "mp4": return "video";
			case "mp3": return "audio";
			case "pdf": return "pdf";
			case "doc":
			case "docx": return "doc";
			case "xls":
			case "xlsx": return "xls";
			case "ppt":
			case "pptx": return "ppt";
			case "rar":
			case "zip":
			case "7z": return "archive";
			case "txt":
			case "md": return "text";
			case "csv": return "csv";
			case "json":
			case "js":
			case "ts":
			case "html":
			case "css": return "code";
			default: return "unknown";
		}
	}
	function Y(t) {
		return t ? Array.isArray(t) ? t.filter(Boolean) : String(t).split(",").map((t) => t.trim()).filter(Boolean) : [];
	}
	function K(t, e) {
		return e ? `[data-tallkit-${t}="${e}"]` : `[data-tallkit-${t}]`;
	}
	function X(t) {
		return null == t ? t : t.replace(/[&<>"']/g, (t) => ({
			"&": "&amp;",
			"<": "&lt;",
			">": "&gt;",
			"\"": "&quot;",
			"'": "&#39;"
		})[t]);
	}
	function J(t, e, i) {
		return Q([
			"tallkit",
			t,
			e ?? Math.random().toString(36).slice(2, 9),
			i
		].filter(Boolean).join("-"), {
			replaceAccents: !0,
			removeSpaces: !0,
			replaceSpaces: "-",
			lowercase: !0,
			mode: "alphanumeric"
		}) ?? "";
	}
	function Q(t, e) {
		if (!e || !t) return t;
		const i = {
			replaceAccents: !1,
			removeSpaces: !1,
			lowercase: !1,
			uppercase: !1,
			mode: void 0,
			...e
		};
		switch (i?.replaceAccents && (t = t.normalize("NFD").replace(/[\u0300-\u036f]/g, "")), i.mode) {
			case "alpha":
				t = t.replace(/[^a-z\s-]/gi, "");
				break;
			case "alphanumeric":
				t = t.replace(/[^a-z0-9\s-]/gi, "");
				break;
			case "numeric": t = t.replace(/[^0-9\s-]/g, "");
		}
		return i?.removeSpaces && (t = t.replace(/\s+/g, " ").trim()), i?.replaceSpaces && (t = t.replace(/\s+/g, i.replaceSpaces).trim()), i.uppercase && !i.lowercase ? t = t.toUpperCase() : i.lowercase && !i.uppercase && (t = t.toLowerCase()), t;
	}
	function Z(t, e) {
		t && (t.value = e?.toString() ?? "", t.dispatchEvent(new Event("input", { bubbles: !0 })), t.dispatchEvent(new Event("change", { bubbles: !0 })));
	}
	function G(t, e) {
		t && t.checked !== e && (t.checked = e, t.dispatchEvent(new Event("input", { bubbles: !0 })), t.dispatchEvent(new Event("change", { bubbles: !0 })));
	}
	function tt(t, e, i = "field") {
		return t?.closest(K(i))?.querySelector(K(e)) ?? null;
	}
	function et(t) {
		return tt(t, "input", "field-control");
	}
	function it(t, e) {
		return t.length > 0 && t.every(e);
	}
	function st() {
		return !!window.Livewire;
	}
	function nt(t) {
		const e = window.Livewire?.hook("commit", t);
		return "function" == typeof e ? e : () => {};
	}
	function rt(t) {
		if (!t) return null;
		for (const e of t.attributes) if (e.name.startsWith("wire:model")) {
			const t = e.name.includes(".") ? e.name.split(".").slice(1).join(".") : "";
			return {
				name: e.value,
				modifier: t
			};
		}
		return null;
	}
	function ot(t, e, i = 500) {
		const s = !e || isNaN(parseInt(e.toString())) ? i : parseInt(e.toString());
		return setTimeout(t, s);
	}
	function at(t, e = 300) {
		let i;
		const s = (...s) => {
			clearTimeout(i), i = setTimeout(() => t(...s), e);
		};
		return s.cancel = () => clearTimeout(i), s;
	}
	var lt = i({ addressForm: () => ht });
	function ht(t = {}) {
		const e = function(t, { ttl: e = 36e5, persist: i = !0 } = {}) {
			const s = /* @__PURE__ */ new Map();
			return {
				getStorageKey: (e) => [
					"tallkit",
					"cache",
					t,
					e
				].filter(Boolean).join(":"),
				get(t) {
					const e = s.get(t);
					if (e) {
						if (Date.now() < e.exp) return e.data;
						s.delete(t);
					}
					if (i) try {
						const e = localStorage.getItem(this.getStorageKey(t));
						if (!e) return null;
						const i = JSON.parse(e);
						return Date.now() > i.exp ? (localStorage.removeItem(this.getStorageKey(t)), null) : (s.set(t, i), i.data);
					} catch (t) {
						return null;
					}
					return null;
				},
				set(t, n) {
					const r = {
						data: n,
						exp: Date.now() + e
					};
					if (s.set(t, r), i) try {
						localStorage.setItem(this.getStorageKey(t), JSON.stringify(r));
					} catch (t) {}
				}
			};
		}("zipcode", t);
		return {
			abortController: null,
			$els: {},
			init() {
				this.$els = {
					loading: this.$root.querySelector(K("loading")),
					zipcode: this.$root.querySelector(K("address-form-zipcode")),
					address: this.$root.querySelector(K("address-form-address")),
					number: this.$root.querySelector(K("address-form-number")),
					complement: this.$root.querySelector(K("address-form-complement")),
					neighborhood: this.$root.querySelector(K("address-form-neighborhood")),
					city: this.$root.querySelector(K("address-form-city")),
					state: this.$root.querySelector(K("address-form-state"))
				};
				const t = at(this.search.bind(this));
				g(this.$els.zipcode, { "@input"() {
					t(this.$el.value);
				} });
			},
			setLoading(t) {
				this.$els.loading?.classList.toggle("hidden", !t), [
					"address",
					"neighborhood",
					"city",
					"state"
				].map((t) => this.$els[t]).filter(Boolean).forEach((e) => e.disabled = t);
			},
			resolveState(t) {
				const e = this.$els.state;
				if (!e) return "";
				const i = t.estado ?? t.uf;
				return "input" === e.tagName.toLowerCase() ? i ?? "" : null != i && Array.from(e.options ?? []).some((t) => t.value === i) ? i : t.uf ?? "";
			},
			normalizeZipcode: (t) => t.replace(/\D/g, ""),
			async viaCep(t, e) {
				const i = await (await fetch(`https://viacep.com.br/ws/${t}/json/`, { signal: e })).json();
				if (i.erro) {
					const t = /* @__PURE__ */ new Error("ViaCEP not found");
					throw t.name = "NotFoundError", t;
				}
				return i;
			},
			async brasilApi(t, e) {
				const i = await fetch(`https://brasilapi.com.br/api/cep/v1/${t}`, { signal: e });
				if (!i.ok) {
					const t = /* @__PURE__ */ new Error("BrasilAPI error");
					throw 404 === i.status && (t.name = "NotFoundError"), t;
				}
				const s = await i.json();
				return {
					logradouro: s.street,
					bairro: s.neighborhood,
					localidade: s.city,
					uf: s.state
				};
			},
			async resolveAddress(t, e) {
				const i = [this.viaCep.bind(this), this.brasilApi.bind(this)];
				for (const s of i) try {
					return await H(() => s(t, e));
				} catch (t) {
					if ("AbortError" === t.name) throw t;
				}
				throw new Error("All providers failed");
			},
			fill(t) {
				Z(this.$els.address, t.logradouro), Z(this.$els.neighborhood, t.bairro), Z(this.$els.city, t.localidade), Z(this.$els.state, this.resolveState(t)), this.$els.number?.focus();
			},
			async search(t) {
				const i = this.normalizeZipcode(t);
				if (this.abortController?.abort(), 8 !== i.length) return;
				const s = new AbortController();
				this.abortController = s;
				const { signal: n } = s, r = e.get(i);
				if (r) {
					if (this.setLoading(!0), await new Promise((t) => setTimeout(t, 120)), n.aborted) return;
					this.fill(r), this.$dispatch("loaded", {
						zipcode: i,
						data: r,
						cached: !0
					}), this.setLoading(!1);
					return;
				}
				this.setLoading(!0), this.$dispatch("loading", { zipcode: i });
				try {
					const t = await this.resolveAddress(i, n);
					if (n.aborted) return;
					e.set(i, t), this.fill(t), this.$dispatch("loaded", {
						zipcode: i,
						data: t,
						cached: !1
					});
				} catch (t) {
					if ("AbortError" === t.name || n.aborted) return;
					this.$dispatch("error", {
						zipcode: i,
						error: t
					}), this.$els.zipcode?.focus();
				} finally {
					n.aborted || this.setLoading(!1);
				}
			},
			destroy() {
				this.abortController?.abort();
			}
		};
	}
	function ct(t) {
		return {
			cancelDismiss: null,
			isDismissing: !1,
			_dismissTimeout: null,
			init() {
				g(this.$root.querySelectorAll(K("dismissible")), { "@click.stop": () => {
					this.dismiss("manual");
				} }), g(this.$root, { "@dismiss": (t) => {
					const e = t.detail || {};
					this.dismiss(e.reason || "programmatic");
				} });
			},
			beforeDismiss() {},
			dismiss(e = "programmatic") {
				if (this.isDismissing) return;
				const i = new CustomEvent("before-dismiss", {
					detail: { reason: e },
					cancelable: !0
				});
				if (this.$root.dispatchEvent(i), i.defaultPrevented) return;
				this.isDismissing = !0, this.beforeDismiss(), this.cancelDismiss?.(), this.cancelDismiss = null;
				const s = () => {
					this.isDismissing = !1, this.cancelDismiss = null, this.$dispatch("dismissed", { reason: e }), this.$root.isConnected && this.$root.remove();
				};
				"fade" === t ? this.cancelDismiss = o(this.$root, { onDone: s }) : "collapse" === t ? this.cancelDismiss = a(this.$root, { onDone: s }) : s(), this._dismissTimeout && clearTimeout(this._dismissTimeout), this._dismissTimeout = setTimeout(() => {
					this.isDismissing = !1, this._dismissTimeout = null;
				}, Math.max(1.5 * n(this.$root), 500));
			},
			destroy() {
				this.cancelDismiss?.(), this.cancelDismiss = null, this.isDismissing = !1, this._dismissTimeout && (clearTimeout(this._dismissTimeout), this._dismissTimeout = null);
			}
		};
	}
	var ut = i({ alertComponent: () => dt });
	function dt({ timeout: t = 0, pauseOnHover: e = !1 } = {}) {
		const i = ct("collapse");
		return {
			...i,
			timeoutId: null,
			remaining: t,
			startedAt: 0,
			pauseReasons: /* @__PURE__ */ new Set(),
			progressEl: null,
			visibilityHandler: null,
			state: "idle",
			init() {
				i.init.call(this), this.progressEl = this.$root.querySelector(K("alert-progress")), this.startTimer(), this.initProgress(), this.visibilityHandler = this.handleVisibility.bind(this), document.addEventListener("visibilitychange", this.visibilityHandler), g(this.$root, {
					...e ? {
						"@mouseenter": () => this.pause("hover"),
						"@mouseleave": () => this.resume("hover")
					} : {},
					"@pause": () => this.pause("external"),
					"@resume": () => this.resume("external")
				});
			},
			startTimer() {
				!t || this.remaining <= 0 || (this.state = "running", this.startedAt = Date.now(), this.timeoutId = ot(() => this.dismiss("timeout"), this.remaining, 7e3));
			},
			pause(t = "manual") {
				if (this.pauseReasons.add(t), this.pauseReasons.size > 1) return;
				if (!this.timeoutId) return;
				const e = Date.now() - this.startedAt;
				this.remaining = Math.max(this.remaining - e, 0), clearTimeout(this.timeoutId), this.timeoutId = null, this.state = "paused", this.freezeProgress();
			},
			resume(t = "manual") {
				this.pauseReasons.delete(t), this.pauseReasons.size > 0 || this.remaining <= 0 || (this.state = "running", this.progressEl && (this.progressEl.style.transitionDuration = "150ms", requestAnimationFrame(() => {
					requestAnimationFrame(() => {
						this.progressEl && (this.progressEl.style.transitionTimingFunction = "linear", this.progressEl.style.transitionDuration = `${this.remaining}ms`, this.applyProgress(0));
					});
				})), this.startTimer());
			},
			handleVisibility() {
				document.hidden ? this.pause("visibility") : this.resume("visibility");
			},
			initProgress() {
				this.progressEl && this.remaining && (this.progressEl.style.transitionTimingFunction = "linear", this.applyProgress(100), requestAnimationFrame(() => {
					this.progressEl && (this.progressEl.style.transitionDuration = `${this.remaining}ms`, this.applyProgress(0));
				}));
			},
			applyProgress(t) {
				this.progressEl && (this.progressEl.style.backgroundSize = `${t}% 100%`);
			},
			freezeProgress() {
				if (!this.progressEl) return;
				const t = getComputedStyle(this.progressEl).backgroundSize;
				this.progressEl.style.transitionDuration = "0ms", this.progressEl.style.backgroundSize = t;
			},
			beforeDismiss() {
				this.state = "dismissing", this.timeoutId && (clearTimeout(this.timeoutId), this.timeoutId = null);
			},
			destroy() {
				this.timeoutId && (clearTimeout(this.timeoutId), this.timeoutId = null), this.visibilityHandler && (document.removeEventListener("visibilitychange", this.visibilityHandler), this.visibilityHandler = null), i.destroy.call(this), this.pauseReasons.clear(), this.state = "idle";
			}
		};
	}
	function pt() {
		return { getDataOptions(t = this.$el) {
			return window.Alpine.evaluate(t, t.getAttribute("data-options") || "{}");
		} };
	}
	var mt = i({ loadable: () => gt });
	function gt() {
		return {
			empty: null,
			loaded: null,
			error: null,
			_loadToken: 0,
			_pendingLoad: null,
			async load(t, e = !1) {
				e || this.$el.hasAttribute("data-silent") || this.start();
				const i = this._loadToken;
				try {
					const e = await t();
					this.complete(0, i), "function" == typeof e && this.$nextTick(e);
				} catch (t) {
					this.fail(t, 0, i);
				}
			},
			reset() {
				this.empty = null, this.loaded = null, this.error = null;
			},
			clear() {
				this.reset(), this.empty = !0;
			},
			start() {
				this._loadToken++, this._clearPendingLoad(), this.reset(), this.loaded = !1, this.$dispatch("started");
			},
			complete(t = 0, e) {
				e ??= this._loadToken, this._clearPendingLoad(), this._pendingLoad = setTimeout(() => {
					this._pendingLoad = null, e === this._loadToken && (this.reset(), this.loaded = !0, this.$dispatch("completed"));
				}, t);
			},
			fail(t, e = 0, i) {
				i ??= this._loadToken, this._clearPendingLoad(), this._pendingLoad = setTimeout(() => {
					this._pendingLoad = null, i === this._loadToken && (this.reset(), this.error = t, this.$dispatch("failed"));
				}, e);
			},
			_clearPendingLoad() {
				this._pendingLoad && (clearTimeout(this._pendingLoad), this._pendingLoad = null);
			},
			destroy() {
				this._clearPendingLoad();
			},
			startAndComplete(t = !1) {
				this.start(), t && this.$nextTick(() => this.complete());
			},
			isEmpty() {
				return !0 === this.empty;
			},
			isLoading() {
				return !1 === this.loaded;
			},
			isCompleted() {
				return !0 === this.loaded;
			},
			isError() {
				return null !== this.error;
			}
		};
	}
	var ft = i({ apexcharts: () => vt });
	function vt() {
		const t = gt();
		return {
			...t,
			...pt(),
			chart: null,
			init() {
				this.load(() => c(() => !!window.ApexCharts, "https://cdn.jsdelivr.net/npm/apexcharts@5"));
			},
			render(t = {}) {
				try {
					const e = {
						...t,
						...this.getDataOptions(this.$refs.target)
					};
					this.chart ? this.chart.updateOptions(e) : (this.chart = new window.ApexCharts(this.$refs.target, e), this.chart.render()), this.$dispatch("rendered", { chart: this.chart });
				} catch (t) {
					this.fail(t);
				}
			},
			destroy() {
				t.destroy.call(this), this.chart?.destroy(), this.chart = null;
			}
		};
	}
	function yt() {
		return {
			_onResize: null,
			_resizeObserver: null,
			init() {
				this.updateOffset(), this._onResize = () => this.updateOffset(), window.addEventListener("resize", this._onResize), this._resizeObserver = new ResizeObserver(() => this.updateOffset()), this._resizeObserver.observe(document.body);
			},
			updateOffset() {
				const t = this.$el.offsetTop;
				this.$el.style.position = "sticky", this.$el.style.top = `${t}px`, this.$el.style.maxHeight = `calc(100dvh - ${t}px)`;
			},
			destroy() {
				window.removeEventListener("resize", this._onResize), this._resizeObserver?.disconnect();
			}
		};
	}
	var wt = i({ aside: () => bt });
	function bt() {
		return { ...yt() };
	}
	function kt() {
		return {
			opened: !1,
			lastOpened: null,
			init(t = !1) {
				if (Number.isInteger(t)) return ot(() => this.open(), t);
				this.opened = Boolean(t);
			},
			open(t = !0) {
				this.opened = !0, t && (this.lastOpened = this.opened);
			},
			close(t = !0) {
				this.opened = !1, t && (this.lastOpened = this.opened);
			},
			toggle(t = !0) {
				this.isOpened() ? this.close(t) : this.open(t);
			},
			isOpened() {
				return !0 === this.opened;
			},
			isClosed() {
				return !1 === this.opened;
			}
		};
	}
	var At = i({ popover: () => $t });
	function $t({ mode: t = "hover", position: e = "bottom", align: i = "end", matchTriggerWidth: s = !1 } = {}) {
		const r = kt();
		return {
			...r,
			popoverElement: null,
			trigger: null,
			ariaTrigger: null,
			resizeObserver: null,
			mutationObserver: null,
			livewireCommitCleanup: null,
			_rAF: null,
			_cancelPendingClose: null,
			mouseX: 0,
			mouseY: 0,
			_hasPointerPosition: !1,
			init() {
				if (r.init.call(this), this.popoverElement = this.$root.lastElementChild?.matches("[popover]") && this.$root.lastElementChild, !this.popoverElement) return;
				this.trigger = this.$root.firstElementChild !== this.popoverElement ? this.$root.firstElementChild : this.$root, this.trigger?.matches(K("tooltip")) && (this.trigger = this.trigger.firstElementChild), this.ariaTrigger = this.trigger?.matches(K("control")) ? this.trigger : this.trigger?.querySelector(K("control")) ?? this.trigger;
				const e = this.popoverElement.getAttribute("role");
				if (this.ariaTrigger.hasAttribute("aria-haspopup") || "tooltip" === e || this.ariaTrigger.setAttribute("aria-haspopup", "listbox" === e || "dialog" === e ? e : "true"), this.ariaTrigger.hasAttribute("aria-expanded") || this.ariaTrigger.setAttribute("aria-expanded", "false"), this.popoverElement.id || (this.popoverElement.id = J("popover")), this.ariaTrigger.hasAttribute("aria-controls") || this.ariaTrigger.setAttribute("aria-controls", this.popoverElement.id), "tooltip" === e) {
					const t = new Set((this.ariaTrigger.getAttribute("aria-describedby") ?? "").split(" ").filter(Boolean));
					t.add(this.popoverElement.id), this.ariaTrigger.setAttribute("aria-describedby", Array.from(t).join(" "));
				}
				this.popoverElement.addEventListener("beforetoggle", (t) => {
					queueMicrotask(() => {
						"open" === t.newState ? this.onOpen() : this.onClose();
					});
				}), this.livewireCommitCleanup = nt(({ succeed: t }) => {
					t(() => {
						this.popoverElement?.matches(":popover-open") && this.$root?.isConnected && this.boundSetPosition();
					});
				}), "manual" === t || !window.matchMedia("(hover: none)").matches && "dropdown" !== t ? "hover" === t ? g(this.trigger, {
					"@mouseenter"() {
						this.open(!1);
					},
					"@mouseleave"() {
						this.close();
					},
					"@focus"() {
						this.open();
					},
					"@blur"() {
						this.close();
					}
				}) : "context" === t && (this.trigger.hasAttribute("tabindex") || [
					"A",
					"BUTTON",
					"INPUT",
					"SELECT",
					"TEXTAREA"
				].includes(this.trigger.tagName) || this.trigger.setAttribute("tabindex", "0"), g(this.trigger, {
					"@contextmenu.prevent"(t) {
						this.close(), this.mouseX = t.clientX, this.mouseY = t.clientY, this._hasPointerPosition = !0, this.open();
					},
					"@keydown"(t) {
						("ContextMenu" === t.key || t.shiftKey && "F10" === t.key) && (t.preventDefault(), this.close(), this._hasPointerPosition = !1, this.open());
					}
				}), g(this.popoverElement, { "@click.outside"() {
					this.close();
				} })) : g(this.trigger, {
					"@click"() {
						this.toggle(!["menu"].includes(e));
					},
					"@click.outside"(t) {
						(this.popoverElement.hasAttribute("data-keep-open") || t.target?.hasAttribute("data-keep-open") || t.target?.closest("[data-keep-open]")) && this.popoverElement.contains(t.target) || this.close();
					}
				}), g(this.trigger, {
					"@open"() {
						this.open();
					},
					"@close"() {
						this.close();
					},
					"@keydown.escape.window"() {
						this.close();
					}
				});
			},
			destroy() {
				this.onClose(), this.livewireCommitCleanup?.();
			},
			open(t = !0) {
				requestAnimationFrame(() => {
					if (this.popoverElement?.isConnected) {
						if (this._cancelPendingClose) this._cancelPendingClose(), this._cancelPendingClose = null, this.onOpen();
						else {
							if (this.popoverElement.matches(":popover-open")) return;
							this.popoverElement.showPopover();
						}
						t && (this.popoverElement.querySelector("[role=menuitem], [role=option], [role=tab]") ?? this.popoverElement).focus();
					}
				});
			},
			close() {
				requestAnimationFrame(() => {
					if (!this.popoverElement?.isConnected) return;
					if (!this.popoverElement.matches(":popover-open")) return;
					if (this._cancelPendingClose) return;
					this.onClose();
					const t = this.popoverElement.firstElementChild ?? this.popoverElement;
					let e;
					const i = () => {
						t.removeEventListener("transitionend", i), clearTimeout(e), this._cancelPendingClose = null, this.popoverElement?.isConnected && this.popoverElement.matches(":popover-open") && this.popoverElement.hidePopover();
					};
					this._cancelPendingClose = () => {
						t.removeEventListener("transitionend", i), clearTimeout(e), this._cancelPendingClose = null;
					}, requestAnimationFrame(() => {
						const s = n(t);
						0 !== s ? (t.addEventListener("transitionend", i, { once: !0 }), e = setTimeout(i, s + 50)) : i();
					});
				});
			},
			onOpen() {
				r.open.call(this), this.ariaTrigger.setAttribute("aria-expanded", "true"), this._onScroll ??= () => this.boundSetPosition(), this._onResize ??= () => this.boundSetPosition(), window.addEventListener("scroll", this._onScroll, !0), window.addEventListener("resize", this._onResize, !0), this.resizeObserver = new ResizeObserver(() => this.boundSetPosition()), this.resizeObserver.observe(this.trigger), this.resizeObserver.observe(this.popoverElement), this.mutationObserver = new MutationObserver(() => this.boundSetPosition()), this.mutationObserver.observe(this.trigger, { childList: !0 }), this.mutationObserver.observe(this.popoverElement, { childList: !0 }), this.setPosition();
			},
			onClose() {
				this.isClosed() || (r.close.call(this), this.ariaTrigger.setAttribute("aria-expanded", "false"), window.removeEventListener("scroll", this._onScroll, !0), window.removeEventListener("resize", this._onResize, !0), this.resizeObserver?.disconnect(), this.resizeObserver = null, this.mutationObserver?.disconnect(), this.mutationObserver = null, this._rAF && (cancelAnimationFrame(this._rAF), this._rAF = null));
			},
			setPosition() {
				if (!this.popoverElement?.isConnected) return;
				if (!this.popoverElement.matches(":popover-open")) return;
				if (!("context" === t && this._hasPointerPosition || this.trigger?.isConnected)) return;
				let n;
				n = "context" === t && this._hasPointerPosition ? {
					top: this.mouseY,
					bottom: this.mouseY,
					left: this.mouseX,
					right: this.mouseX,
					height: 0,
					width: 0
				} : this.trigger.getBoundingClientRect();
				const r = n.height, o = n.width;
				s && (this.popoverElement.style.width = `${o}px`);
				const a = window.scrollY, l = window.scrollX, h = this.popoverElement.offsetHeight, c = this.popoverElement.offsetWidth, u = W(this.trigger), d = (t) => "start" === t ? u ? "right" : "left" : "end" === t ? u ? "left" : "right" : t, p = (t, e) => {
					const i = ((t, e) => "left" === (e = d(e)) ? 0 : "right" === e ? "left" === t || "right" === t ? r - h : o - c : "left" === t || "right" === t ? (r - h) / 2 : (o - c) / 2)(t, e);
					let s = 0, u = 0;
					switch (t) {
						case "right":
							u = n.right + 4 + l, s = n.top + i + a;
							break;
						case "left":
							u = n.left - c - 4 + l, s = n.top + i + a;
							break;
						case "bottom":
							s = n.bottom + 4 + a, u = n.left + i + l;
							break;
						case "top": s = n.top - h - 4 + a, u = n.left + i + l;
					}
					return {
						top: s,
						left: u
					};
				}, m = ({ top: t, left: e }) => t >= a && e >= l && t + h <= a + window.innerHeight && e + c <= l + window.innerWidth, g = [
					"top",
					"bottom",
					"left",
					"right"
				], f = [
					"start",
					"left",
					"end",
					"right",
					"center"
				];
				let v = e || "bottom", y = i || "end", w = p(v, y);
				if (!m(w)) {
					let t = !1;
					for (const e of [v, ...g.filter((t) => t !== v)]) {
						for (const i of [y, ...f.filter((t) => t !== y)]) {
							const s = p(e, i);
							if (m(s)) {
								v = e, y = i, w = s, t = !0;
								break;
							}
						}
						if (t) break;
					}
				}
				this.popoverElement.style.position = "absolute", this.popoverElement.style.inset = "auto", this.popoverElement.style.top = `${w.top}px`, this.popoverElement.style.left = `${w.left}px`, this.popoverElement.dataset.position = v, this.popoverElement.dataset.align = "center" === y ? "center" : d(y);
			},
			boundSetPosition() {
				this._rAF || (this._rAF = requestAnimationFrame(() => {
					this.setPosition(), this._rAF = null;
				}));
			}
		};
	}
	function xt(t) {
		return Array.isArray ? Array.isArray(t) : "[object Array]" === It(t);
	}
	function Et(t) {
		return null == t ? "" : function(t) {
			if ("string" == typeof t) return t;
			if ("bigint" == typeof t) return t.toString();
			const e = t + "";
			return "0" == e && 1 / t == -1 / 0 ? "-0" : e;
		}(t);
	}
	function Ct(t) {
		return "string" == typeof t;
	}
	function St(t) {
		return "number" == typeof t;
	}
	function Dt(t) {
		return !0 === t || !1 === t || function(t) {
			return Mt(t) && null !== t;
		}(t) && "[object Boolean]" == It(t);
	}
	function Mt(t) {
		return "object" == typeof t;
	}
	function Tt(t) {
		return null != t;
	}
	function Ft(t) {
		return !t.trim().length;
	}
	function It(t) {
		return null == t ? void 0 === t ? "[object Undefined]" : "[object Null]" : Object.prototype.toString.call(t);
	}
	var _t = "Invalid doc index: must be a non-negative integer within the bounds of the docs array", Lt = Object.prototype.hasOwnProperty, Bt = class {
		constructor(t) {
			this._keys = [], this._keyMap = {};
			let e = 0;
			t.forEach((t) => {
				const i = Ot(t);
				this._keys.push(i), this._keyMap[i.id] = i, e += i.weight;
			}), this._keys.forEach((t) => {
				t.weight /= e;
			});
		}
		get(t) {
			return this._keyMap[t];
		}
		keys() {
			return this._keys;
		}
		toJSON() {
			return JSON.stringify(this._keys);
		}
	};
	function Ot(t) {
		let e = null, i = null, s = null, n = 1, r = null;
		if (Ct(t) || xt(t)) s = t, e = jt(t), i = zt(t);
		else {
			if (!Lt.call(t, "name")) throw new Error(((t) => `Missing ${t} property in key`)("name"));
			const o = t.name;
			if (s = o, Lt.call(t, "weight") && void 0 !== t.weight && (n = t.weight, n <= 0)) throw new Error(((t) => `Property 'weight' in key '${t}' must be a positive integer`)(zt(o)));
			e = jt(o), i = zt(o), r = t.getFn ?? null;
		}
		return {
			path: e,
			id: i,
			weight: n,
			src: s,
			getFn: r
		};
	}
	function jt(t) {
		return xt(t) ? t : t.split(".");
	}
	function zt(t) {
		return xt(t) ? t.join(".") : t;
	}
	var Nt = Object.freeze({
		isCaseSensitive: !1,
		ignoreDiacritics: !1,
		includeScore: !1,
		keys: [],
		shouldSort: !0,
		sortFn: (t, e) => t.score === e.score ? t.idx < e.idx ? -1 : 1 : t.score < e.score ? -1 : 1,
		includeMatches: !1,
		findAllMatches: !1,
		minMatchCharLength: 1,
		location: 0,
		threshold: .6,
		distance: 100,
		useExtendedSearch: !1,
		useTokenSearch: !1,
		tokenize: void 0,
		tokenMatch: "any",
		getFn: function(t, e) {
			const i = [];
			let s = !1;
			const n = (t, e, r, o) => {
				if (Tt(t)) if (e[r]) {
					const a = t[e[r]];
					if (!Tt(a)) return;
					if (r === e.length - 1 && (Ct(a) || St(a) || Dt(a) || "bigint" == typeof a)) i.push(void 0 !== o ? {
						v: Et(a),
						i: o
					} : Et(a));
					else if (xt(a)) {
						s = !0;
						for (let t = 0, i = a.length; t < i; t += 1) n(a[t], e, r + 1, t);
					} else e.length && n(a, e, r + 1, o);
				} else i.push(void 0 !== o ? {
					v: t,
					i: o
				} : t);
			};
			return n(t, Ct(e) ? e.split(".") : e, 0), s ? i : i[0];
		},
		ignoreLocation: !1,
		ignoreFieldNorm: !1,
		fieldNormWeight: 1
	});
	function Rt(t) {
		return t >= 9 && t <= 13 || 32 === t || 160 === t;
	}
	var Pt = class {
		constructor({ getFn: t = Nt.getFn, fieldNormWeight: e = Nt.fieldNormWeight } = {}) {
			this.norm = function(t = 1, e = 3) {
				const i = /* @__PURE__ */ new Map(), s = Math.pow(10, e);
				return {
					get(e) {
						let n = 0, r = !1;
						for (let t = 0; t < e.length; t++) Rt(e.charCodeAt(t)) ? r = !1 : r || (n++, r = !0);
						if (0 === n && (n = 1), i.has(n)) return i.get(n);
						const o = Math.round(s / Math.pow(n, .5 * t)) / s;
						return i.set(n, o), o;
					},
					clear() {
						i.clear();
					}
				};
			}(e, 3), this.getFn = t, this.isCreated = !1, this.docs = [], this.keys = [], this._keysMap = {}, this.setIndexRecords();
		}
		setSources(t = []) {
			this.docs = t;
		}
		setIndexRecords(t = []) {
			this.records = t;
		}
		setKeys(t = []) {
			this.keys = t, this._keysMap = {}, t.forEach((t, e) => {
				this._keysMap[t.id] = e;
			});
		}
		create() {
			if (this.isCreated || !this.docs.length) return;
			this.isCreated = !0;
			const t = this.docs.length;
			this.records = new Array(t);
			let e = 0;
			if (Ct(this.docs[0])) for (let i = 0; i < t; i++) {
				const t = this._createStringRecord(this.docs[i], i);
				t && (this.records[e++] = t);
			}
			else for (let i = 0; i < t; i++) this.records[e++] = this._createObjectRecord(this.docs[i], i);
			this.records.length = e, this.norm.clear();
		}
		add(t, e) {
			if (!Number.isInteger(e) || e < 0) throw new Error(_t);
			if (Ct(t)) {
				const i = this._createStringRecord(t, e);
				return i && this.records.push(i), i;
			}
			const i = this._createObjectRecord(t, e);
			return this.records.push(i), i;
		}
		removeAt(t) {
			if (!Number.isInteger(t) || t < 0) throw new Error(_t);
			for (let e = 0, i = this.records.length; e < i; e += 1) if (this.records[e].i === t) {
				this.records.splice(e, 1);
				break;
			}
			for (let e = 0, i = this.records.length; e < i; e += 1) this.records[e].i > t && (this.records[e].i -= 1);
		}
		removeAll(t) {
			const e = /* @__PURE__ */ new Set();
			for (const i of t) Number.isInteger(i) && i >= 0 && e.add(i);
			if (0 === e.size) return;
			this.records = this.records.filter((t) => !e.has(t.i));
			const i = Array.from(e).sort((t, e) => t - e);
			for (const t of this.records) {
				let e = 0, s = i.length;
				for (; e < s;) {
					const n = e + s >>> 1;
					i[n] < t.i ? e = n + 1 : s = n;
				}
				t.i -= e;
			}
		}
		getValueForItemAtKeyId(t, e) {
			return t[this._keysMap[e]];
		}
		size() {
			return this.records.length;
		}
		_createStringRecord(t, e) {
			return !Tt(t) || Ft(t) ? null : {
				v: t,
				i: e,
				n: this.norm.get(t)
			};
		}
		_createObjectRecord(t, e) {
			const i = {
				i: e,
				$: {}
			};
			for (let e = 0, s = this.keys.length; e < s; e++) {
				const s = this.keys[e], n = s.getFn ? s.getFn(t) : this.getFn(t, s.path);
				if (Tt(n)) {
					if (xt(n)) {
						const t = [];
						for (let e = 0, i = n.length; e < i; e += 1) {
							const i = n[e];
							if (Tt(i)) {
								if (Ct(i)) {
									if (!Ft(i)) {
										const s = {
											v: i,
											i: e,
											n: this.norm.get(i)
										};
										t.push(s);
									}
								} else if (Tt(i.v)) {
									const e = Ct(i.v) ? i.v : Et(i.v);
									if (!Ft(e)) {
										const s = {
											v: e,
											i: i.i,
											n: this.norm.get(e)
										};
										t.push(s);
									}
								}
							}
						}
						i.$[e] = t;
					} else if (Ct(n) && !Ft(n)) {
						const t = {
							v: n,
							n: this.norm.get(n)
						};
						i.$[e] = t;
					}
				}
			}
			return i;
		}
		toJSON() {
			return {
				keys: this.keys.map(({ getFn: t, ...e }) => e),
				records: this.records
			};
		}
	};
	function Ut(t, e, { getFn: i = Nt.getFn, fieldNormWeight: s = Nt.fieldNormWeight } = {}) {
		const n = new Pt({
			getFn: i,
			fieldNormWeight: s
		});
		return n.setKeys(t.map(Ot)), n.setSources(e), n.create(), n;
	}
	function Wt(t, e, i, { location: s = Nt.location, distance: n = Nt.distance, threshold: r = Nt.threshold, findAllMatches: o = Nt.findAllMatches, minMatchCharLength: a = Nt.minMatchCharLength, includeMatches: l = Nt.includeMatches, ignoreLocation: h = Nt.ignoreLocation } = {}) {
		if (e.length > 32) throw new Error(`Pattern length exceeds max of 32.`);
		const c = e.length, u = t.length, d = Math.max(0, Math.min(s, u));
		let p = r, m = d;
		const g = (t, e) => {
			const i = t / c;
			if (h) return i;
			const s = Math.abs(d - e);
			return n ? i + s / n : s ? 1 : i;
		}, f = a > 1 || l, v = f ? Array(u) : [];
		let y;
		for (; (y = t.indexOf(e, m)) > -1;) {
			const t = g(0, y);
			if (p = Math.min(t, p), m = y + c, f) {
				let t = 0;
				for (; t < c;) v[y + t] = 1, t += 1;
			}
		}
		m = -1;
		let w = [], b = 1, k = 0, A = c + u;
		const $ = 1 << c - 1;
		for (let e = 0; e < c; e += 1) {
			let s = 0, n = A;
			for (; s < n;) g(e, d + n) <= p ? s = n : A = n, n = Math.floor((A - s) / 2 + s);
			A = n;
			let r = Math.max(1, d - n + 1);
			const a = o ? u : Math.min(d + n, u) + c, l = Array(a + 2);
			l[a + 1] = (1 << e) - 1;
			for (let s = a; s >= r; s -= 1) {
				const n = s - 1, o = i[t[n]];
				if (l[s] = (l[s + 1] << 1 | 1) & o, e && (l[s] |= (w[s + 1] | w[s]) << 1 | 1 | w[s + 1]), l[s] & $ && (b = g(e, n), b <= p)) {
					if (p = b, m = n, k = e, m <= d) break;
					r = Math.max(1, 2 * d - m);
				}
			}
			if (g(e + 1, d) > p) break;
			w = l;
		}
		if (f && m >= 0) {
			const e = Math.min(u - 1, m + c - 1 + k);
			for (let s = m; s <= e; s += 1) i[t[s]] && (v[s] = 1);
		}
		const x = {
			isMatch: m >= 0,
			score: Math.max(.001, b)
		};
		if (f) {
			const t = function(t = [], e = Nt.minMatchCharLength) {
				const i = [];
				let s = -1, n = -1, r = 0;
				for (let o = t.length; r < o; r += 1) {
					const o = t[r];
					o && -1 === s ? s = r : o || -1 === s || (n = r - 1, n - s + 1 >= e && i.push([s, n]), s = -1);
				}
				return t[r - 1] && r - s >= e && i.push([s, r - 1]), i;
			}(v, a);
			t.length ? l && (x.indices = t) : x.isMatch = !1;
		}
		return x;
	}
	function Ht(t) {
		const e = {};
		for (let i = 0, s = t.length; i < s; i += 1) {
			const n = t.charAt(i);
			e[n] = (e[n] || 0) | 1 << s - i - 1;
		}
		return e;
	}
	function Vt(t) {
		if (t.length <= 1) return t;
		t.sort((t, e) => t[0] - e[0] || t[1] - e[1]);
		const e = [t[0]];
		for (let i = 1, s = t.length; i < s; i += 1) {
			const s = e[e.length - 1], n = t[i];
			n[0] <= s[1] + 1 ? s[1] = Math.max(s[1], n[1]) : e.push(n);
		}
		return e;
	}
	var Yt = {
		"ł": "l",
		"Ł": "L",
		"đ": "d",
		"Đ": "D",
		"ø": "o",
		"Ø": "O",
		"ħ": "h",
		"Ħ": "H",
		"ŧ": "t",
		"Ŧ": "T",
		"ı": "i",
		"ß": "ss"
	}, Kt = new RegExp("[" + Object.keys(Yt).join("") + "]", "g"), Xt = "function" == typeof String.prototype.normalize ? (t) => t.normalize("NFD").replace(/[\u0300-\u036F\u0483-\u0489\u0591-\u05BD\u05BF\u05C1\u05C2\u05C4\u05C5\u05C7\u0610-\u061A\u064B-\u065F\u0670\u06D6-\u06DC\u06DF-\u06E4\u06E7\u06E8\u06EA-\u06ED\u0711\u0730-\u074A\u07A6-\u07B0\u07EB-\u07F3\u07FD\u0816-\u0819\u081B-\u0823\u0825-\u0827\u0829-\u082D\u0859-\u085B\u08D3-\u08E1\u08E3-\u0903\u093A-\u093C\u093E-\u094F\u0951-\u0957\u0962\u0963\u0981-\u0983\u09BC\u09BE-\u09C4\u09C7\u09C8\u09CB-\u09CD\u09D7\u09E2\u09E3\u09FE\u0A01-\u0A03\u0A3C\u0A3E-\u0A42\u0A47\u0A48\u0A4B-\u0A4D\u0A51\u0A70\u0A71\u0A75\u0A81-\u0A83\u0ABC\u0ABE-\u0AC5\u0AC7-\u0AC9\u0ACB-\u0ACD\u0AE2\u0AE3\u0AFA-\u0AFF\u0B01-\u0B03\u0B3C\u0B3E-\u0B44\u0B47\u0B48\u0B4B-\u0B4D\u0B56\u0B57\u0B62\u0B63\u0B82\u0BBE-\u0BC2\u0BC6-\u0BC8\u0BCA-\u0BCD\u0BD7\u0C00-\u0C04\u0C3E-\u0C44\u0C46-\u0C48\u0C4A-\u0C4D\u0C55\u0C56\u0C62\u0C63\u0C81-\u0C83\u0CBC\u0CBE-\u0CC4\u0CC6-\u0CC8\u0CCA-\u0CCD\u0CD5\u0CD6\u0CE2\u0CE3\u0D00-\u0D03\u0D3B\u0D3C\u0D3E-\u0D44\u0D46-\u0D48\u0D4A-\u0D4D\u0D57\u0D62\u0D63\u0D82\u0D83\u0DCA\u0DCF-\u0DD4\u0DD6\u0DD8-\u0DDF\u0DF2\u0DF3\u0E31\u0E34-\u0E3A\u0E47-\u0E4E\u0EB1\u0EB4-\u0EB9\u0EBB\u0EBC\u0EC8-\u0ECD\u0F18\u0F19\u0F35\u0F37\u0F39\u0F3E\u0F3F\u0F71-\u0F84\u0F86\u0F87\u0F8D-\u0F97\u0F99-\u0FBC\u0FC6\u102B-\u103E\u1056-\u1059\u105E-\u1060\u1062-\u1064\u1067-\u106D\u1071-\u1074\u1082-\u108D\u108F\u109A-\u109D\u135D-\u135F\u1712-\u1714\u1732-\u1734\u1752\u1753\u1772\u1773\u17B4-\u17D3\u17DD\u180B-\u180D\u1885\u1886\u18A9\u1920-\u192B\u1930-\u193B\u1A17-\u1A1B\u1A55-\u1A5E\u1A60-\u1A7C\u1A7F\u1AB0-\u1ABE\u1B00-\u1B04\u1B34-\u1B44\u1B6B-\u1B73\u1B80-\u1B82\u1BA1-\u1BAD\u1BE6-\u1BF3\u1C24-\u1C37\u1CD0-\u1CD2\u1CD4-\u1CE8\u1CED\u1CF2-\u1CF4\u1CF7-\u1CF9\u1DC0-\u1DF9\u1DFB-\u1DFF\u20D0-\u20F0\u2CEF-\u2CF1\u2D7F\u2DE0-\u2DFF\u302A-\u302F\u3099\u309A\uA66F-\uA672\uA674-\uA67D\uA69E\uA69F\uA6F0\uA6F1\uA802\uA806\uA80B\uA823-\uA827\uA880\uA881\uA8B4-\uA8C5\uA8E0-\uA8F1\uA8FF\uA926-\uA92D\uA947-\uA953\uA980-\uA983\uA9B3-\uA9C0\uA9E5\uAA29-\uAA36\uAA43\uAA4C\uAA4D\uAA7B-\uAA7D\uAAB0\uAAB2-\uAAB4\uAAB7\uAAB8\uAABE\uAABF\uAAC1\uAAEB-\uAAEF\uAAF5\uAAF6\uABE3-\uABEA\uABEC\uABED\uFB1E\uFE00-\uFE0F\uFE20-\uFE2F]/g, "").replace(Kt, (t) => Yt[t]) : (t) => t, Jt = class {
		constructor(t, { location: e = Nt.location, threshold: i = Nt.threshold, distance: s = Nt.distance, includeMatches: n = Nt.includeMatches, findAllMatches: r = Nt.findAllMatches, minMatchCharLength: o = Nt.minMatchCharLength, isCaseSensitive: a = Nt.isCaseSensitive, ignoreDiacritics: l = Nt.ignoreDiacritics, ignoreLocation: h = Nt.ignoreLocation } = {}) {
			if (this.options = {
				location: e,
				threshold: i,
				distance: s,
				includeMatches: n,
				findAllMatches: r,
				minMatchCharLength: o,
				isCaseSensitive: a,
				ignoreDiacritics: l,
				ignoreLocation: h
			}, t = a ? t : t.toLowerCase(), t = l ? Xt(t) : t, this.pattern = t, this.chunks = [], !this.pattern.length) return;
			const c = (t, e) => {
				this.chunks.push({
					pattern: t,
					alphabet: Ht(t),
					startIndex: e
				});
			}, u = this.pattern.length;
			if (u > 32) {
				let t = 0;
				const e = u % 32, i = u - e;
				for (; t < i;) c(this.pattern.substr(t, 32), t), t += 32;
				if (e) {
					const t = u - 32;
					c(this.pattern.substr(t), t);
				}
			} else c(this.pattern, 0);
		}
		searchIn(t) {
			const { isCaseSensitive: e, ignoreDiacritics: i, includeMatches: s } = this.options;
			if (t = e ? t : t.toLowerCase(), t = i ? Xt(t) : t, this.pattern === t) {
				if (t.length < this.options.minMatchCharLength) return {
					isMatch: !1,
					score: 1
				};
				const e = {
					isMatch: !0,
					score: 0
				};
				return s && (e.indices = [[0, t.length - 1]]), e;
			}
			const { location: n, distance: r, threshold: o, findAllMatches: a, minMatchCharLength: l, ignoreLocation: h } = this.options, c = [];
			let u = 0, d = !1;
			this.chunks.forEach(({ pattern: e, alphabet: i, startIndex: p }) => {
				const { isMatch: m, score: g, indices: f } = Wt(t, e, i, {
					location: n + p,
					distance: r,
					threshold: o,
					findAllMatches: a,
					minMatchCharLength: l,
					includeMatches: s,
					ignoreLocation: h
				});
				m && (d = !0), u += g, m && f && c.push(...f);
			});
			const p = {
				isMatch: d,
				score: d ? u / this.chunks.length : 1
			};
			return d && s && (p.indices = Vt(c)), p;
		}
	}, Qt = /* @__PURE__ */ new Set(["fuzzy", "include"]);
	function Zt(t) {
		return t.startsWith("inverse");
	}
	var Gt = [
		{
			type: "exact",
			multiRegex: /^="(.*)"$/,
			singleRegex: /^=(.*)$/,
			create: (t) => ({
				type: "exact",
				search(e) {
					const i = e === t;
					return {
						isMatch: i,
						score: i ? 0 : 1,
						indices: [0, t.length - 1]
					};
				}
			})
		},
		{
			type: "include",
			multiRegex: /^'"(.*)"$/,
			singleRegex: /^'(.*)$/,
			create: (t) => ({
				type: "include",
				search(e) {
					let i, s = 0;
					const n = [], r = t.length;
					for (; (i = e.indexOf(t, s)) > -1;) s = i + r, n.push([i, s - 1]);
					const o = !!n.length;
					return {
						isMatch: o,
						score: o ? 0 : 1,
						indices: n
					};
				}
			})
		},
		{
			type: "prefix-exact",
			multiRegex: /^\^"(.*)"$/,
			singleRegex: /^\^(.*)$/,
			create: (t) => ({
				type: "prefix-exact",
				search(e) {
					const i = e.startsWith(t);
					return {
						isMatch: i,
						score: i ? 0 : 1,
						indices: [0, t.length - 1]
					};
				}
			})
		},
		{
			type: "inverse-prefix-exact",
			multiRegex: /^!\^"(.*)"$/,
			singleRegex: /^!\^(.*)$/,
			create: (t) => ({
				type: "inverse-prefix-exact",
				search(e) {
					const i = !e.startsWith(t);
					return {
						isMatch: i,
						score: i ? 0 : 1,
						indices: [0, e.length - 1]
					};
				}
			})
		},
		{
			type: "inverse-suffix-exact",
			multiRegex: /^!"(.*)"\$$/,
			singleRegex: /^!(.*)\$$/,
			create: (t) => ({
				type: "inverse-suffix-exact",
				search(e) {
					const i = !e.endsWith(t);
					return {
						isMatch: i,
						score: i ? 0 : 1,
						indices: [0, e.length - 1]
					};
				}
			})
		},
		{
			type: "suffix-exact",
			multiRegex: /^"(.*)"\$$/,
			singleRegex: /^(.*)\$$/,
			create: (t) => ({
				type: "suffix-exact",
				search(e) {
					const i = e.endsWith(t);
					return {
						isMatch: i,
						score: i ? 0 : 1,
						indices: [e.length - t.length, e.length - 1]
					};
				}
			})
		},
		{
			type: "inverse-exact",
			multiRegex: /^!"(.*)"$/,
			singleRegex: /^!(.*)$/,
			create: (t) => ({
				type: "inverse-exact",
				search(e) {
					const i = -1 === e.indexOf(t);
					return {
						isMatch: i,
						score: i ? 0 : 1,
						indices: [0, e.length - 1]
					};
				}
			})
		},
		{
			type: "fuzzy",
			multiRegex: /^"(.*)"$/,
			singleRegex: /^(.*)$/,
			create: (t, e = {}) => {
				const i = new Jt(t, {
					location: e.location ?? Nt.location,
					threshold: e.threshold ?? Nt.threshold,
					distance: e.distance ?? Nt.distance,
					includeMatches: e.includeMatches ?? Nt.includeMatches,
					findAllMatches: e.findAllMatches ?? Nt.findAllMatches,
					minMatchCharLength: e.minMatchCharLength ?? Nt.minMatchCharLength,
					isCaseSensitive: e.isCaseSensitive ?? Nt.isCaseSensitive,
					ignoreDiacritics: e.ignoreDiacritics ?? Nt.ignoreDiacritics,
					ignoreLocation: e.ignoreLocation ?? Nt.ignoreLocation
				});
				return {
					type: "fuzzy",
					search: (t) => i.searchIn(t)
				};
			}
		}
	], te = Gt.length;
	function ee(t, e) {
		const i = t.match(e);
		return i ? i[1] : null;
	}
	var ie = [];
	function se(...t) {
		ie.push(...t);
	}
	function ne(t, e) {
		for (let i = 0, s = ie.length; i < s; i += 1) {
			const s = ie[i];
			if (s.condition(t, e)) return new s(t, e);
		}
		return new Jt(t, e);
	}
	var re = "$and", oe = "$or", ae = "$path", le = "$val", he = (t) => !(!t[re] && !t[oe]), ce = (t) => ({ [re]: Object.keys(t).map((e) => ({ [e]: t[e] })) });
	function ue(t, e, { auto: i = !0 } = {}) {
		const s = (t) => {
			if (Ct(t)) {
				const s = {
					keyId: null,
					pattern: t
				};
				return i && (s.searcher = ne(t, e)), s;
			}
			const n = Object.keys(t), r = ((t) => !!t[ae])(t);
			if (!r && n.length > 1 && !he(t)) return s(ce(t));
			if (((t) => !xt(t) && Mt(t) && !he(t))(t)) {
				const s = r ? t[ae] : n[0], o = r ? t[le] : t[s];
				if (!Ct(o)) throw new Error(((t) => `Invalid value for key ${t}`)(s));
				const a = {
					keyId: zt(s),
					pattern: o
				};
				return i && (a.searcher = ne(o, e)), a;
			}
			const o = {
				children: [],
				operator: n[0]
			};
			return n.forEach((e) => {
				const i = t[e];
				xt(i) && i.forEach((t) => {
					o.children.push(s(t));
				});
			}), o;
		};
		return he(t) || (t = ce(t)), s(t);
	}
	function de(t, { ignoreFieldNorm: e = Nt.ignoreFieldNorm }) {
		let i = 1;
		return t.forEach(({ key: t, norm: s, score: n }) => {
			const r = t ? t.weight : null;
			i *= Math.pow(0 === n && r ? Number.EPSILON : n, (r || 1) * (e ? 1 : s));
		}), i;
	}
	var pe = class {
		constructor(t, e) {
			this.limit = t, this.heap = [], this.comparator = e;
		}
		get size() {
			return this.heap.length;
		}
		insert(t) {
			this.size < this.limit ? (this.heap.push(t), this._bubbleUp(this.size - 1)) : this.comparator(t, this.heap[0]) < 0 && (this.heap[0] = t, this._sinkDown(0));
		}
		extractSorted() {
			return this.heap.sort(this.comparator);
		}
		_bubbleUp(t) {
			const e = this.heap;
			for (; t > 0;) {
				const i = t - 1 >> 1;
				if (this.comparator(e[t], e[i]) <= 0) break;
				const s = e[t];
				e[t] = e[i], e[i] = s, t = i;
			}
		}
		_sinkDown(t) {
			const e = this.heap, i = e.length;
			let s = t;
			do {
				const n = 2 * (t = s) + 1, r = 2 * t + 2;
				if (n < i && this.comparator(e[n], e[s]) > 0 && (s = n), r < i && this.comparator(e[r], e[s]) > 0 && (s = r), s !== t) {
					const i = e[t];
					e[t] = e[s], e[s] = i;
				}
			} while (s !== t);
		}
	};
	function me(t, e, { includeMatches: i = Nt.includeMatches, includeScore: s = Nt.includeScore } = {}) {
		return t.map((t) => {
			const { idx: n } = t, r = {
				item: e[n],
				refIndex: n
			};
			return i && (r.matches = function(t) {
				const e = [];
				return t.matches.forEach((t) => {
					if (!Tt(t.indices) || !t.indices.length) return;
					const i = {
						indices: t.indices,
						value: t.value
					};
					t.key && (i.key = t.key.id), t.idx > -1 && (i.refIndex = t.idx), e.push(i);
				}), e;
			}(t)), s && (r.score = t.score), r;
		});
	}
	var ge = /[\p{L}\p{M}\p{N}_]+/gu, fe = /* @__PURE__ */ new WeakSet();
	function ve(t) {
		if ("function" == typeof t) {
			let e = !1;
			return (i) => {
				const s = t(i);
				if (!e && (e = !0, !Array.isArray(s) || s.some((t) => "string" != typeof t))) throw new Error(`[Fuse] tokenize function must return string[]; received ${Array.isArray(s) ? "array containing non-strings" : typeof s}.`);
				return s;
			};
		}
		return t instanceof RegExp ? (t.global || (e = t, fe.has(e) || (fe.add(e), console.warn(`[Fuse] tokenize regex ${e} lacks the global flag; only the first match per text will be returned. Add the 'g' flag.`))), (e) => e.match(t) || []) : (t) => t.match(ge) || [];
		var e;
	}
	function ye({ isCaseSensitive: t = !1, ignoreDiacritics: e = !1, tokenize: i } = {}) {
		const s = ve(i);
		return { tokenize: (i) => (t || (i = i.toLowerCase()), e && (i = Xt(i)), s(i)) };
	}
	function we(t, e, i, s) {
		const n = s.tokenize(e);
		if (!n.length) return;
		t.fieldCount++, t.docFieldCount.set(i, (t.docFieldCount.get(i) || 0) + 1);
		const r = new Set(n);
		let o = t.docTermFieldHits.get(i);
		o || (o = /* @__PURE__ */ new Map(), t.docTermFieldHits.set(i, o));
		for (const e of r) o.set(e, (o.get(e) || 0) + 1), t.df.set(e, (t.df.get(e) || 0) + 1);
	}
	function be(t, e, i, s) {
		const { i: n, v: r, $: o } = e;
		if (void 0 === r) {
			if (o) for (let e = 0; e < i; e++) {
				const i = o[e];
				if (i) if (Array.isArray(i)) for (const e of i) we(t, e.v, n, s);
				else we(t, i.v, n, s);
			}
		} else we(t, r, n, s);
	}
	function ke(t, e) {
		const i = t.docFieldCount.get(e);
		if (void 0 === i) return;
		t.fieldCount -= i, t.docFieldCount.delete(e);
		const s = t.docTermFieldHits.get(e);
		if (s) {
			for (const [e, i] of s) {
				const s = (t.df.get(e) || 0) - i;
				s <= 0 ? t.df.delete(e) : t.df.set(e, s);
			}
			t.docTermFieldHits.delete(e);
		}
	}
	function Ae(t, e) {
		if (0 === e.length) return;
		const i = Array.from(new Set(e)).sort((t, e) => t - e);
		for (const e of i) ke(t, e);
		const s = (t) => {
			let e = 0, s = i.length;
			for (; e < s;) {
				const n = e + s >>> 1;
				i[n] < t ? e = n + 1 : s = n;
			}
			return t - e;
		}, n = i[0], r = /* @__PURE__ */ new Map();
		for (const [e, i] of t.docFieldCount) r.set(e > n ? s(e) : e, i);
		t.docFieldCount = r;
		const o = /* @__PURE__ */ new Map();
		for (const [e, i] of t.docTermFieldHits) o.set(e > n ? s(e) : e, i);
		t.docTermFieldHits = o;
	}
	var $e = class {
		constructor(t, e, i) {
			this.options = {
				...Nt,
				...e
			}, this.options.useExtendedSearch, this.options.useTokenSearch, this._keyStore = new Bt(this.options.keys), this._docs = t, this._myIndex = null, this._invertedIndex = null, this.setCollection(t, i), this._lastQuery = null, this._lastSearcher = null;
		}
		_getSearcher(t) {
			if (this._lastQuery === t) return this._lastSearcher;
			const e = ne(t, this._invertedIndex ? {
				...this.options,
				_invertedIndex: this._invertedIndex
			} : this.options);
			return this._lastQuery = t, this._lastSearcher = e, e;
		}
		setCollection(t, e) {
			if (this._docs = t, e && !(e instanceof Pt)) throw new Error("Incorrect 'index' type");
			if (this._myIndex = e || Ut(this.options.keys, this._docs, {
				getFn: this.options.getFn,
				fieldNormWeight: this.options.fieldNormWeight
			}), this.options.useTokenSearch) {
				const t = ye({
					isCaseSensitive: this.options.isCaseSensitive,
					ignoreDiacritics: this.options.ignoreDiacritics,
					tokenize: this.options.tokenize
				});
				this._invertedIndex = function(t, e, i) {
					const s = {
						fieldCount: 0,
						df: /* @__PURE__ */ new Map(),
						docFieldCount: /* @__PURE__ */ new Map(),
						docTermFieldHits: /* @__PURE__ */ new Map()
					};
					for (const n of t) be(s, n, e, i);
					return s;
				}(this._myIndex.records, this._myIndex.keys.length, t);
			}
			this._invalidateSearcherCache();
		}
		add(t) {
			if (!Tt(t)) return;
			this._docs.push(t);
			const e = this._myIndex.add(t, this._docs.length - 1);
			if (this._invertedIndex && e) {
				const t = ye({
					isCaseSensitive: this.options.isCaseSensitive,
					ignoreDiacritics: this.options.ignoreDiacritics,
					tokenize: this.options.tokenize
				});
				(function(t, e, i, s) {
					be(t, e, i, s);
				})(this._invertedIndex, e, this._myIndex.keys.length, t);
			}
			this._invalidateSearcherCache();
		}
		remove(t = () => !1) {
			const e = [], i = [];
			for (let s = 0, n = this._docs.length; s < n; s += 1) t(this._docs[s], s) && (e.push(this._docs[s]), i.push(s));
			if (i.length) {
				this._invertedIndex && Ae(this._invertedIndex, i);
				const t = new Set(i);
				this._docs = this._docs.filter((e, i) => !t.has(i)), this._myIndex.removeAll(i), this._invalidateSearcherCache();
			}
			return e;
		}
		removeAt(t) {
			if (!Number.isInteger(t) || t < 0 || t >= this._docs.length) throw new Error(_t);
			this._invertedIndex && Ae(this._invertedIndex, [t]);
			const e = this._docs.splice(t, 1)[0];
			return this._myIndex.removeAt(t), this._invalidateSearcherCache(), e;
		}
		_invalidateSearcherCache() {
			this._lastQuery = null, this._lastSearcher = null;
		}
		getIndex() {
			return this._myIndex;
		}
		_normalizedKeys() {
			return this._myIndex.keys.map((t) => this._keyStore.get(t.id) || t);
		}
		search(t, e) {
			const { limit: i = -1 } = e || {}, { includeMatches: s, includeScore: n, shouldSort: r, sortFn: o, ignoreFieldNorm: a } = this.options;
			if (Ct(t) && !t.trim()) {
				let t = this._docs.map((t, e) => ({
					item: t,
					refIndex: e
				}));
				return St(i) && i > -1 && (t = t.slice(0, i)), t;
			}
			const l = r && St(i) && i > 0 && Ct(t), h = o, c = (t, e) => h(t, e) || t.idx - e.idx;
			let u;
			if (l) {
				const e = new pe(i, c);
				Ct(this._docs[0]) ? this._searchStringList(t, {
					heap: e,
					ignoreFieldNorm: a
				}) : this._searchObjectList(t, {
					heap: e,
					ignoreFieldNorm: a
				}), u = e.extractSorted();
			} else u = Ct(t) ? Ct(this._docs[0]) ? this._searchStringList(t) : this._searchObjectList(t) : this._searchLogical(t), function(t, { ignoreFieldNorm: e = Nt.ignoreFieldNorm }) {
				t.forEach((t) => {
					t.score = de(t.matches, { ignoreFieldNorm: e });
				});
			}(u, { ignoreFieldNorm: a }), r && u.sort(Ct(t) ? c : h), St(i) && i > -1 && (u = u.slice(0, i));
			return me(u, this._docs, {
				includeMatches: s,
				includeScore: n
			});
		}
		_searchStringList(t, { heap: e, ignoreFieldNorm: i } = {}) {
			const s = this._getSearcher(t), n = this.options.useTokenSearch && "all" === this.options.tokenMatch, { records: r } = this._myIndex, o = e ? null : [];
			return r.forEach(({ v: t, i: r, n: a }) => {
				if (!Tt(t)) return;
				const l = s.searchIn(t);
				if (l.isMatch) {
					const s = {
						score: l.score,
						value: t,
						norm: a,
						indices: l.indices
					};
					n && (s.matchedMask = l.matchedMask, s.matchedTerms = l.matchedTerms, s.termCount = l.termCount);
					const h = [s];
					if (!n || this._coversAllTokens(h)) {
						const s = {
							item: t,
							idx: r,
							matches: h
						};
						e ? (s.score = de(s.matches, { ignoreFieldNorm: i }), e.insert(s)) : o.push(s);
					}
				}
			}), o;
		}
		_searchLogical(t) {
			const e = ue(t, this.options), i = this._normalizedKeys(), s = (t, e, n) => {
				if (!("children" in t)) {
					const { keyId: s, searcher: r } = t;
					let o;
					return null === s ? (o = [], i.forEach((t, i) => {
						o.push(...this._findMatches({
							key: t,
							value: e[i],
							searcher: r
						}));
					})) : o = this._findMatches({
						key: this._keyStore.get(s),
						value: this._myIndex.getValueForItemAtKeyId(e, s),
						searcher: r
					}), o && o.length ? [{
						idx: n,
						item: e,
						matches: o
					}] : [];
				}
				const { children: r, operator: o } = t, a = [];
				for (let t = 0, i = r.length; t < i; t += 1) {
					const i = r[t], l = s(i, e, n);
					if (l.length) a.push(...l);
					else if (o === re) return [];
				}
				return a;
			}, n = this._myIndex.records, r = /* @__PURE__ */ new Map(), o = [];
			return n.forEach(({ $: t, i }) => {
				if (Tt(t)) {
					const n = s(e, t, i);
					n.length && (r.has(i) || (r.set(i, {
						idx: i,
						item: t,
						matches: []
					}), o.push(r.get(i))), n.forEach(({ matches: t }) => {
						r.get(i).matches.push(...t);
					}));
				}
			}), o;
		}
		_searchObjectList(t, { heap: e, ignoreFieldNorm: i } = {}) {
			const s = this._getSearcher(t), n = this.options.useTokenSearch && "all" === this.options.tokenMatch, { records: r } = this._myIndex, o = this._normalizedKeys(), a = e ? null : [];
			return r.forEach(({ $: t, i: r }) => {
				if (!Tt(t)) return;
				const l = [];
				let h = !1, c = !1;
				if (o.forEach((e, i) => {
					const n = this._findMatches({
						key: e,
						value: t[i],
						searcher: s
					});
					n.length ? (l.push(...n), n[0].hasInverse && (c = !0)) : h = !0;
				}), (!c || !h) && l.length && (!n || this._coversAllTokens(l))) {
					const s = {
						idx: r,
						item: t,
						matches: l
					};
					e ? (s.score = de(s.matches, { ignoreFieldNorm: i }), e.insert(s)) : a.push(s);
				}
			}), a;
		}
		_findMatches({ key: t, value: e, searcher: i }) {
			if (!Tt(e)) return [];
			const s = [];
			if (xt(e)) e.forEach(({ v: e, i: n, n: r }) => {
				if (!Tt(e)) return;
				const o = i.searchIn(e);
				if (o.isMatch) {
					const i = {
						score: o.score,
						key: t,
						value: e,
						idx: n,
						norm: r,
						indices: o.indices,
						hasInverse: o.hasInverse
					};
					void 0 !== o.termCount && (i.matchedMask = o.matchedMask, i.matchedTerms = o.matchedTerms, i.termCount = o.termCount), s.push(i);
				}
			});
			else {
				const { v: n, n: r } = e, o = i.searchIn(n);
				if (o.isMatch) {
					const e = {
						score: o.score,
						key: t,
						value: n,
						norm: r,
						indices: o.indices,
						hasInverse: o.hasInverse
					};
					void 0 !== o.termCount && (e.matchedMask = o.matchedMask, e.matchedTerms = o.matchedTerms, e.termCount = o.termCount), s.push(e);
				}
			}
			return s;
		}
		_coversAllTokens(t) {
			const e = t.length ? t[0].termCount : void 0;
			if (void 0 === e) return !0;
			if (e <= 31) {
				let i = 0;
				for (let e = 0; e < t.length; e++) i |= t[e].matchedMask || 0;
				return i === 2 ** e - 1;
			}
			const i = /* @__PURE__ */ new Set();
			for (let e = 0; e < t.length; e++) {
				const s = t[e].matchedTerms;
				if (s) for (const t of s) i.add(t);
			}
			return i.size === e;
		}
	};
	$e.version = "7.5.0", $e.createIndex = Ut, $e.parseIndex = function(t, { getFn: e = Nt.getFn, fieldNormWeight: i = Nt.fieldNormWeight } = {}) {
		const { keys: s, records: n } = t, r = new Pt({
			getFn: e,
			fieldNormWeight: i
		});
		return r.setKeys(s), r.setIndexRecords(n), r;
	}, $e.config = Nt, $e.match = function(t, e, i) {
		if (i && i.useTokenSearch) throw new Error("Fuse.match does not support useTokenSearch: token search requires corpus-level statistics (df, fieldCount) that a one-off string comparison does not have. Use new Fuse(...).search(...) instead.");
		return ne(t, {
			...Nt,
			...i
		}).searchIn(e);
	}, $e.parseQuery = ue, se(class {
		constructor(t, { isCaseSensitive: e = Nt.isCaseSensitive, ignoreDiacritics: i = Nt.ignoreDiacritics, includeMatches: s = Nt.includeMatches, minMatchCharLength: n = Nt.minMatchCharLength, ignoreLocation: r = Nt.ignoreLocation, findAllMatches: o = Nt.findAllMatches, location: a = Nt.location, threshold: l = Nt.threshold, distance: h = Nt.distance } = {}) {
			this.query = null, this.options = {
				isCaseSensitive: e,
				ignoreDiacritics: i,
				includeMatches: s,
				minMatchCharLength: n,
				findAllMatches: o,
				ignoreLocation: r,
				location: a,
				threshold: l,
				distance: h
			}, t = e ? t : t.toLowerCase(), t = i ? Xt(t) : t, this.pattern = t, this.query = function(t, e = {}) {
				return t.replace(/\\\|/g, "\0").split("|").map((t) => {
					const i = function(t) {
						const e = [], i = t.length;
						let s = 0;
						for (; s < i;) {
							for (; s < i && " " === t[s];) s++;
							if (s >= i) break;
							let n = s;
							for (; n < i && " " !== t[n] && "\"" !== t[n];) n++;
							if (n < i && "\"" === t[n]) {
								for (n++; n < i;) {
									if ("\"" === t[n]) {
										const e = n + 1;
										if (e >= i || " " === t[e]) {
											n++;
											break;
										}
										if ("$" === t[e] && (e + 1 >= i || " " === t[e + 1])) {
											n += 2;
											break;
										}
									}
									n++;
								}
								e.push(t.substring(s, n)), s = n;
							} else {
								for (; n < i && " " !== t[n];) n++;
								e.push(t.substring(s, n)), s = n;
							}
						}
						return e;
					}(t.replace(/\u0000/g, "|").trim()).filter((t) => t && !!t.trim()), s = [];
					for (let t = 0, n = i.length; t < n; t += 1) {
						const n = i[t];
						let r = !1, o = -1;
						for (; !r && ++o < te;) {
							const t = Gt[o], i = ee(n, t.multiRegex);
							i && (s.push(t.create(i, e)), r = !0);
						}
						if (!r) for (o = -1; ++o < te;) {
							const t = Gt[o], i = ee(n, t.singleRegex);
							if (i) {
								s.push(t.create(i, e));
								break;
							}
						}
					}
					return s;
				});
			}(this.pattern, this.options);
		}
		static condition(t, e) {
			return e.useExtendedSearch;
		}
		searchIn(t) {
			const e = this.query;
			if (!e) return {
				isMatch: !1,
				score: 1
			};
			const { includeMatches: i, isCaseSensitive: s, ignoreDiacritics: n } = this.options;
			t = s ? t : t.toLowerCase(), t = n ? Xt(t) : t;
			let r = 0;
			const o = [];
			let a = 0, l = !1;
			for (let s = 0, n = e.length; s < n; s += 1) {
				const n = e[s];
				o.length = 0, r = 0, l = !1;
				for (let e = 0, s = n.length; e < s; e += 1) {
					const s = n[e], { isMatch: h, indices: c, score: u } = s.search(t);
					if (!h) {
						a = 0, r = 0, o.length = 0, l = !1;
						break;
					}
					r += 1, a += u, Zt(s.type) && (l = !0), i && (Qt.has(s.type) ? o.push(...c) : o.push(c));
				}
				if (r) {
					const t = {
						isMatch: !0,
						score: a / r
					};
					return l && (t.hasInverse = !0), i && (t.indices = Vt(o)), t;
				}
			}
			return {
				isMatch: !1,
				score: 1
			};
		}
	}), se(class {
		static condition(t, e) {
			return e.useTokenSearch;
		}
		constructor(t, e) {
			this.options = e, this.analyzer = ye({
				isCaseSensitive: e.isCaseSensitive,
				ignoreDiacritics: e.ignoreDiacritics,
				tokenize: e.tokenize
			});
			const i = this.analyzer.tokenize(t), { df: s, fieldCount: n } = e._invertedIndex;
			this.termSearchers = [], this.idfWeights = [];
			for (const t of i) {
				this.termSearchers.push(new Jt(t, {
					location: e.location,
					threshold: e.threshold,
					distance: e.distance,
					includeMatches: e.includeMatches,
					findAllMatches: e.findAllMatches,
					minMatchCharLength: e.minMatchCharLength,
					isCaseSensitive: e.isCaseSensitive,
					ignoreDiacritics: e.ignoreDiacritics,
					ignoreLocation: !0
				}));
				const i = s.get(t) || 0, r = Math.log(1 + (n - i + .5) / (i + .5));
				this.idfWeights.push(r);
			}
			this.combineAll = "all" === e.tokenMatch, this.numTerms = this.termSearchers.length, this.useMask = this.numTerms <= 31;
		}
		searchIn(t) {
			if (!this.termSearchers.length) return {
				isMatch: !1,
				score: 1
			};
			const e = [];
			let i = 0, s = 0, n = 0, r = 0;
			const o = this.combineAll && !this.useMask ? /* @__PURE__ */ new Set() : null;
			for (let a = 0; a < this.termSearchers.length; a++) {
				const l = this.termSearchers[a].searchIn(t), h = this.idfWeights[a];
				s += h, l.isMatch && (n++, i += h * (1 - l.score), l.indices && e.push(...l.indices), this.combineAll && (this.useMask ? r |= 1 << a : o.add(a)));
			}
			if (0 === n) return {
				isMatch: !1,
				score: 1
			};
			const a = s > 0 ? 1 - i / s : 0, l = {
				isMatch: !0,
				score: Math.max(.001, a)
			};
			return this.options.includeMatches && e.length && (l.indices = Vt(e)), this.combineAll && (this.useMask ? l.matchedMask = r : l.matchedTerms = o, l.termCount = this.numTerms), l;
		}
	}), $e.use = function(...t) {
		t.forEach((t) => se(t));
	};
	var xe = $e, Ee = i({ listbox: () => Ce });
	function Ce({ hideEmpty: t = !1, clearOnSelect: e = !1, ...i } = {}) {
		return {
			input: null,
			list: null,
			noRecords: null,
			items: [],
			filteredItems: [],
			index: null,
			fuse: null,
			lastInteraction: null,
			debouncedSearch: null,
			livewireCommitCleanup: null,
			init() {
				this.input = this.$root.querySelector(K("input")), this.list = this.$root.querySelector("[role=listbox]"), this.noRecords = this.$root.querySelector("[role=status]"), this.refreshItems(), this.livewireCommitCleanup = nt(({ succeed: t }) => {
					t(() => {
						this.$root?.isConnected && (this.refreshItems(), this.search());
					});
				}), this.$watch(() => this.index, (t) => {
					this.setActive(t);
				}), this.debouncedSearch = at(() => this.search(), 150), g(this.input, {
					"@input"() {
						this.lastInteraction = "keyboard", this.$dispatch("listbox-search-updated", { query: this.input.value }), this.debouncedSearch();
					},
					"@focus"() {
						this.search();
					},
					"@blur"() {
						this.clear();
					},
					"@keydown.escape.prevent"() {
						this.clear();
					},
					"@keydown.arrow-up.prevent"() {
						this.lastInteraction = "keyboard", this.prev();
					},
					"@keydown.arrow-down.prevent"() {
						this.lastInteraction = "keyboard", this.next();
					},
					"@keydown.home.prevent"() {
						this.lastInteraction = "keyboard", this.first();
					},
					"@keydown.end.prevent"() {
						this.lastInteraction = "keyboard", this.last();
					},
					"@keydown.enter.prevent"() {
						this.select(this.index);
					},
					"@keydown.tab"() {
						this.select(this.index);
					}
				}), g(this.list, {
					"@mouseleave": () => this.clear(),
					"@mousedown": (t) => {
						const e = t.target.closest("[role=option]");
						if (!e) return;
						const i = Number(e.dataset.index);
						Number.isNaN(i) || this.select(i);
					},
					"@mousemove": (t) => {
						if ("keyboard" === this.lastInteraction && 0 === t.movementX && 0 === t.movementY) return;
						this.lastInteraction = "mouse";
						const e = t.target.closest("[role=option]");
						if (!e) return;
						const i = Number(e.dataset.index);
						Number.isNaN(i) || this.isDisabled(this.filteredItems[i]) || this.index !== i && (this.index = i);
					},
					"@keydown.escape.prevent"() {
						this.clear();
					},
					"@keydown.arrow-up.prevent"() {
						this.lastInteraction = "keyboard", this.prev();
					},
					"@keydown.arrow-down.prevent"() {
						this.lastInteraction = "keyboard", this.next();
					},
					"@keydown.home.prevent"() {
						this.lastInteraction = "keyboard", this.first();
					},
					"@keydown.end.prevent"() {
						this.lastInteraction = "keyboard", this.last();
					},
					"@keydown.enter.prevent"() {
						this.select(this.index);
					},
					"@keydown.space.prevent"() {
						this.select(this.index);
					}
				}), this.$nextTick(() => {
					this.search(), this.$dispatch("listbox-initialized");
				});
			},
			destroy() {
				this.livewireCommitCleanup?.();
			},
			refreshItems() {
				this.items = Array.from(this.list.querySelectorAll("[role=option]")).map((t) => (t.hidden = !0, t?.firstElementChild?.disabled ? t.setAttribute("aria-disabled", "true") : t.removeAttribute("aria-disabled"), {
					title: Q(t.querySelector("[data-item-content]")?.textContent, { removeSpaces: !0 }),
					el: t.firstElementChild,
					li: t
				}));
				const t = xe.createIndex(["title"], this.items);
				this.fuse = new xe(this.items, {
					ignoreDiacritics: !0,
					includeScore: !0,
					threshold: .1,
					keys: ["title"],
					...i
				}, t);
			},
			search() {
				const e = this.input ? this.input.value.trim() : "";
				if (this.clear(), !e.length && t) return void (this.filteredItems = []);
				this.items.forEach((t) => {
					t.li.hidden = !0;
				});
				const i = document.createDocumentFragment();
				let s = [];
				e ? s = this.fuse.search(e) : t || (s = this.items.map((t) => ({ item: t }))), this.filteredItems = s.map((t, e) => {
					const s = t.item.li;
					return s.hidden = !1, s.dataset.index = String(e), i.appendChild(s), t.item;
				}), this.list.appendChild(i), this.$dispatch("listbox-items-changed", {
					list: this.list,
					items: this.items,
					filteredItems: this.filteredItems
				}), this.filteredItems.length && e.length && this.$nextTick(() => {
					this.index = 0;
				}), this.toggleNoRecords();
			},
			isDisabled: (t) => !!t?.el?.hasAttribute("disabled"),
			prev() {
				if (0 === this.filteredItems.length) return;
				let t = null === this.index ? this.filteredItems.length - 1 : (this.index - 1 + this.filteredItems.length) % this.filteredItems.length;
				for (let e = 0; e < this.filteredItems.length && this.isDisabled(this.filteredItems[t]); e++) t = (t - 1 + this.filteredItems.length) % this.filteredItems.length;
				this.isDisabled(this.filteredItems[t]) || (this.index = t);
			},
			next() {
				if (0 === this.filteredItems.length) return;
				let t = null === this.index ? 0 : (this.index + 1) % this.filteredItems.length;
				for (let e = 0; e < this.filteredItems.length && this.isDisabled(this.filteredItems[t]); e++) t = (t + 1) % this.filteredItems.length;
				this.isDisabled(this.filteredItems[t]) || (this.index = t);
			},
			first() {
				if (0 === this.filteredItems.length) return;
				let t = 0;
				for (; t < this.filteredItems.length && this.isDisabled(this.filteredItems[t]);) t++;
				t >= this.filteredItems.length || (this.index = t);
			},
			last() {
				if (0 === this.filteredItems.length) return;
				let t = this.filteredItems.length - 1;
				for (; t >= 0 && this.isDisabled(this.filteredItems[t]);) t--;
				t < 0 || (this.index = t);
			},
			select(t) {
				if (null === t) return;
				const i = this.filteredItems[t];
				if (!i) return;
				const s = i.el;
				s && !s.hasAttribute("disabled") && (s.dispatchEvent(new Event("click", { bubbles: !0 })), e && Z(this.input, ""), this.$dispatch("listbox-item-selected", {
					index: t,
					item: i,
					button: s
				}));
			},
			setActive(t) {
				if (this.clearActive(), null === t) return;
				const e = this.filteredItems[t];
				e && (e.el.dataset.active = "true", e.li.setAttribute("aria-selected", "true"), e.li.hasAttribute("id") && this.list.setAttribute("aria-activedescendant", e.li.getAttribute("id")), e.li.scrollIntoView({ block: "nearest" }), this.$dispatch("listbox-active-changed", {
					index: t,
					item: e
				}));
			},
			clearActive() {
				this.filteredItems.forEach((t) => {
					delete t.el.dataset.active, t.li.removeAttribute("aria-selected");
				}), this.list.removeAttribute("aria-activedescendant");
			},
			clear() {
				this.debouncedSearch?.cancel(), this.clearActive(), this.index = null;
			},
			toggleNoRecords() {
				this.noRecords && (0 === this.filteredItems.length && this.input?.value && !t ? (this.noRecords.removeAttribute("hidden"), this.list.setAttribute("hidden", "")) : (this.noRecords.setAttribute("hidden", ""), this.list.removeAttribute("hidden")));
			}
		};
	}
	var Se = i({ autocomplete: () => De });
	function De(t = {}) {
		const e = $t({
			mode: "manual",
			position: "bottom",
			align: "start",
			matchTriggerWidth: !0
		}), i = Ce({
			hideEmpty: !0,
			clearOnSelect: !1,
			...t
		});
		return {
			...e,
			...i,
			init() {
				e.init.call(this), i.init.call(this), this.trigger = this.input, g(this.input, {
					"@blur"() {
						this.close();
					},
					"@keydown.escape.prevent"() {
						this.close();
					}
				}), g(this.$root, { "@listbox-item-selected"({ detail: t }) {
					Z(this.input, t.item.title), this.close();
				} });
			},
			search() {
				i.search.call(this), this.filteredItems.length ? this.open() : this.close();
			},
			open() {
				e.open.call(this, !1);
			},
			close() {
				e.close.call(this), this.clear();
			}
		};
	}
	var Me = i({ badge: () => Te });
	function Te() {
		return { ...ct("fade") };
	}
	function Fe({ key: t, property: e = "value", serialize: i = function() {
		return this[e] ?? null;
	}, deserialize: s = function(t) {
		return t || null;
	} } = {}) {
		return {
			field: null,
			init() {
				if (this.field = this.$root.querySelector(K(t)), this.field) {
					if (this.$wire) {
						const t = rt(this.field);
						t && (this[e] = s.call(this, this.$wire.get(t.name) ?? null), this.$wire.$watch(t.name, () => {
							this[e] = s.call(this, this.field.value || null);
						}));
					}
					this.$watch(e, () => Z(this.field, i.call(this)));
				}
			}
		};
	}
	var Ie = i({ calendar: () => _e });
	function _e({ value: t = null, multiple: e = !1, mode: i = null, months: s = 1, min: n = null, max: r = null, unavailable: o = null, minRange: a = null, maxRange: l = null, static: h = !1, navigation: c = !0, withToday: u = !1, selectableHeader: d = !1, fixedWeeks: p = !1, startDay: m = null, openTo: g = null, weekNumbers: f = !1, locale: v = null } = {}) {
		s = Math.max(1, Number(s) || 1), a = Number(a) || null, l = Number(l) || null;
		const y = Fe({
			key: "calendar-field",
			serialize() {
				return this.valueString();
			},
			deserialize(t) {
				return this.parseInitialValue(t);
			}
		});
		return {
			...y,
			static: h,
			navigation: c,
			withToday: u,
			selectableHeader: d,
			fixedWeeks: p,
			weekNumbers: f,
			locale: v || ("undefined" != typeof navigator ? navigator.language : "en-US"),
			startDay: 0,
			unavailable: Y(o),
			value: null,
			anchorMonth: null,
			focused: null,
			hoverIso: null,
			rangeAnchor: null,
			init() {
				this.startDay = null != m ? Number(m) : function(t) {
					try {
						const e = new Intl.Locale(t).weekInfo ?? new Intl.Locale(t).getWeekInfo?.();
						if (e?.firstDay) return e.firstDay % 7;
					} catch {}
					return 0;
				}(this.locale), this.value = this.parseInitialValue(t), y.init.call(this), this.anchorMonth = F(this.firstAnchorDate()), this.focused = this.firstSelectedIso() ?? M(/* @__PURE__ */ new Date());
			},
			parseInitialValue(t) {
				return "range" === i ? this.normalizeRange(t) : e ? this.normalizeMultiple(t) : this.normalizeSingle(t);
			},
			normalizeSingle: (t) => t && "object" != typeof t ? String(t).trim() || null : Array.isArray(t) ? t[0] ?? null : null,
			normalizeMultiple: (t) => t ? Array.isArray(t) ? t.filter(Boolean) : Y(t) : [],
			normalizeRange(t) {
				if (!t) return null;
				if (Array.isArray(t)) return t[0] || t[1] ? {
					start: t[0] ?? null,
					end: t[1] ?? null
				} : null;
				if ("object" == typeof t) return t.start || t.end ? {
					start: t.start ?? null,
					end: t.end ?? null
				} : null;
				const [e, i] = String(t).split("/");
				return e?.trim() ? {
					start: e.trim(),
					end: i?.trim() || null
				} : null;
			},
			valueString() {
				return "range" === i ? this.value?.start ? this.value.end ? `${this.value.start}/${this.value.end}` : this.value.start : null : e ? (this.value ?? []).join(",") : this.value ?? null;
			},
			firstAnchorDate() {
				const t = this.firstSelectedIso();
				return t ? T(t) : g ? T(g) ?? /* @__PURE__ */ new Date() : /* @__PURE__ */ new Date();
			},
			firstSelectedIso() {
				return "range" === i ? this.value?.start ?? null : e ? this.value?.[0] ?? null : this.value ?? null;
			},
			monthAt(t) {
				return I(this.anchorMonth, t);
			},
			isMonthVisible(t) {
				for (let e = 0; e < s; e++) if (O(this.monthAt(e), t)) return !0;
				return !1;
			},
			weekdayLabels() {
				const t = new Intl.DateTimeFormat(this.locale, { weekday: "short" }), e = new Date(1970, 0, 4);
				return Array.from({ length: 7 }, (i, s) => {
					const n = new Date(e);
					return n.setDate(e.getDate() + (this.startDay + s) % 7), t.format(n);
				});
			},
			monthLabel(t) {
				return new Intl.DateTimeFormat(this.locale, {
					month: "long",
					year: "numeric"
				}).format(this.monthAt(t));
			},
			dayAriaLabel(t) {
				return new Intl.DateTimeFormat(this.locale, { dateStyle: "full" }).format(T(t));
			},
			monthOptions() {
				const t = new Intl.DateTimeFormat(this.locale, { month: "long" });
				return Array.from({ length: 12 }, (e, i) => ({
					value: i,
					label: t.format(new Date(2e3, i, 1))
				}));
			},
			yearOptions() {
				const t = this.anchorMonth.getFullYear();
				return Array.from({ length: 21 }, (e, i) => t - 10 + i);
			},
			weeksFor(t) {
				const e = this.monthAt(t), i = e.getFullYear(), s = e.getMonth(), n = new Date(i, s, 1), r = new Date(i, s + 1, 0).getDate(), o = (n.getDay() - this.startDay + 7) % 7;
				let a = 7 * Math.ceil((o + r) / 7);
				this.fixedWeeks && (a = Math.max(a, 42));
				const l = Array.from({ length: a }, (t, e) => {
					const n = new Date(i, s, e - o + 1);
					return {
						iso: M(n),
						label: n.getDate(),
						inMonth: n.getMonth() === s
					};
				}), h = [];
				for (let t = 0; t < l.length; t += 7) {
					const e = l.slice(t, t + 7), i = e[(4 - this.startDay + 7) % 7];
					h.push({
						key: e[0].iso,
						weekNumber: this.weekNumbers ? z(T(i.iso)) : null,
						days: e
					});
				}
				return h;
			},
			currentMonthIndex() {
				return this.anchorMonth.getMonth();
			},
			setCurrentMonthIndex(t) {
				!this.static && this.navigation && (this.anchorMonth = new Date(this.anchorMonth.getFullYear(), Number(t), 1));
			},
			currentYear() {
				return this.anchorMonth.getFullYear();
			},
			setCurrentYear(t) {
				!this.static && this.navigation && (this.anchorMonth = new Date(Number(t), this.anchorMonth.getMonth(), 1));
			},
			prevMonth() {
				!this.static && this.navigation && (this.anchorMonth = I(this.anchorMonth, -1));
			},
			nextMonth() {
				!this.static && this.navigation && (this.anchorMonth = I(this.anchorMonth, 1));
			},
			goToToday() {
				if (this.static) return;
				const t = /* @__PURE__ */ new Date();
				if (this.isMonthVisible(t)) this.selectDate(M(t));
				else {
					if (!this.navigation) return;
					this.anchorMonth = F(t);
				}
			},
			isDayDisabled(t) {
				return !!this.static || !!(n && t < n) || !!(r && t > r) || !!this.unavailable.includes(t) || !!this.isOutOfRangeSpan(t);
			},
			isOutOfRangeSpan(t) {
				if ("range" !== i) return !1;
				if (!a && !l) return !1;
				if (!this.rangeAnchor || t === this.rangeAnchor) return !1;
				const e = j(this.rangeAnchor <= t ? this.rangeAnchor : t, this.rangeAnchor <= t ? t : this.rangeAnchor) + 1;
				return !!(a && e < a) || !!(l && e > l);
			},
			isUnavailable(t) {
				return !this.static && this.isDayDisabled(t);
			},
			isSelected(t) {
				return "range" === i ? this.value?.start === t || this.value?.end === t : e ? (this.value ?? []).includes(t) : this.value === t;
			},
			isToday: (t) => t === M(/* @__PURE__ */ new Date()),
			displayRange() {
				if ("range" !== i) return null;
				const t = this.value?.start ?? null, e = this.value?.end ?? (this.rangeAnchor ? this.hoverIso : null);
				return t ? e ? t <= e ? {
					lo: t,
					hi: e
				} : {
					lo: e,
					hi: t
				} : {
					lo: t,
					hi: t
				} : null;
			},
			isRangeStart(t) {
				const e = this.displayRange();
				return !!e && e.lo === t;
			},
			isRangeEnd(t) {
				const e = this.displayRange();
				return !!e && e.hi === t;
			},
			isInRange(t) {
				const e = this.displayRange();
				return !!e && t > e.lo && t < e.hi;
			},
			selectDate(t) {
				this.static || this.isDayDisabled(t) || ("range" !== i ? e ? this.toggleMultiple(t) : (this.value = this.value === t ? null : t, this.focused = t, this.$dispatch("calendar-picked", { value: this.value })) : this.pickRangeDate(t));
			},
			toggleMultiple(t) {
				const e = this.value ?? [];
				this.value = e.includes(t) ? e.filter((e) => e !== t) : [...e, t].sort(), this.focused = t, this.$dispatch("calendar-picked", { value: this.value });
			},
			pickRangeDate(t) {
				if (!this.rangeAnchor) return this.rangeAnchor = t, this.value = {
					start: t,
					end: null
				}, void (this.focused = t);
				let [e, i] = this.rangeAnchor <= t ? [this.rangeAnchor, t] : [t, this.rangeAnchor];
				const s = j(e, i) + 1;
				if (a && s < a || l && s > l || this.rangeContainsUnavailable(e, i)) return this.rangeAnchor = t, this.value = {
					start: t,
					end: null
				}, void (this.focused = t);
				this.value = {
					start: e,
					end: i
				}, this.rangeAnchor = null, this.hoverIso = null, this.focused = t, this.$dispatch("calendar-picked", { value: this.value });
			},
			setRangeBound(t, e) {
				if ("range" !== i) return;
				let s = {
					...this.value ?? {
						start: null,
						end: null
					},
					[t]: e || null
				};
				if (s.start && s.end && s.start > s.end && (s = {
					start: s.end,
					end: s.start
				}), (s.start !== (this.value?.start ?? null) || s.end !== (this.value?.end ?? null)) && !(s.start && this.isDayDisabled(s.start) || s.end && this.isDayDisabled(s.end))) {
					if (s.start && s.end) {
						const t = j(s.start, s.end) + 1;
						if (a && t < a || l && t > l || this.rangeContainsUnavailable(s.start, s.end)) return;
					}
					this.value = s, this.rangeAnchor = null, this.hoverIso = null, this.focused = e || this.focused, this.$dispatch("calendar-picked", { value: this.value });
				}
			},
			rangeContainsUnavailable(t, e) {
				if (!this.unavailable.length) return !1;
				for (let i = t; i <= e; i = B(i, 1)) if (this.unavailable.includes(i)) return !0;
				return !1;
			},
			previewRange(t) {
				"range" === i && this.rangeAnchor && (this.hoverIso = t);
			},
			clear() {
				this.value = "range" === i ? null : e ? [] : null, this.rangeAnchor = null, this.hoverIso = null;
			},
			onCellKeydown(t, e) {
				const i = W(this.$root), s = {
					ArrowLeft: i ? 1 : -1,
					ArrowRight: i ? -1 : 1,
					ArrowUp: -7,
					ArrowDown: 7
				};
				t.key in s ? (t.preventDefault(), this.focusIso(B(e, s[t.key]))) : "Home" === t.key ? (t.preventDefault(), this.focusIso(this.weekEdge(e, "start"))) : "End" === t.key ? (t.preventDefault(), this.focusIso(this.weekEdge(e, "end"))) : "PageUp" === t.key ? (t.preventDefault(), this.focusIso(this.shiftMonth(e, t.shiftKey ? -12 : -1))) : "PageDown" === t.key ? (t.preventDefault(), this.focusIso(this.shiftMonth(e, t.shiftKey ? 12 : 1))) : "Enter" !== t.key && " " !== t.key || (t.preventDefault(), this.selectDate(e));
			},
			weekEdge(t, e) {
				const i = (T(t).getDay() - this.startDay + 7) % 7;
				return B(t, "start" === e ? -i : 6 - i);
			},
			shiftMonth(t, e) {
				const i = T(t);
				return M(new Date(i.getFullYear(), i.getMonth() + e, i.getDate()));
			},
			focusIso(t) {
				const e = F(T(t));
				if (!this.isMonthVisible(e)) {
					if (!this.navigation) return;
					this.anchorMonth = e > this.anchorMonth ? I(e, -(s - 1)) : e;
				}
				this.focused = t, this.$nextTick(() => {
					this.$root.querySelector(`[data-iso="${t}"]`)?.focus();
				});
			}
		};
	}
	var Le = i({ chartjs: () => Be });
	function Be() {
		const t = gt();
		return {
			...t,
			...pt(),
			chart: null,
			init() {
				this.load(() => c(() => !!window.Chart, "https://cdn.jsdelivr.net/npm/chart.js@4"));
			},
			render(t = {}) {
				try {
					const e = {
						...t,
						...this.getDataOptions(this.$refs.target)
					};
					this.chart ? (Object.assign(this.chart.config, e), this.chart.update()) : this.chart = new window.Chart(this.$refs.target, e), this.$dispatch("rendered", { chart: this.chart });
				} catch (t) {
					this.fail(t);
				}
			},
			destroy() {
				t.destroy.call(this), this.chart?.destroy(), this.chart = null;
			}
		};
	}
	function Oe(t, e) {
		return {
			all: null,
			items: () => Array.from(document.querySelectorAll(K(`${t}-group`, e))),
			init() {
				this.all = this.$root.querySelector(K(t)), g(this.all, { "@change": () => this.toggleAllItems() }), g(this.items(), { "@change": () => this.updateState() });
			},
			toggleAllItems() {
				this.items().forEach((t) => {
					t.checked = !!this.all?.checked, t.dispatchEvent(new Event("change", { bubbles: !0 }));
				});
			},
			updateState() {
				if (!this.all) return;
				const e = this.items();
				if (this.all.checked = it(e, (t) => t.checked), "checkbox" === t) {
					const t = e.filter((t) => t.checked).length;
					this.all.indeterminate = t > 0 && t < e.length;
				}
			}
		};
	}
	var je = i({ checkboxAll: () => ze });
	function ze({ group: t = "" } = {}) {
		return Oe("checkbox", t);
	}
	var qe = i({ clearable: () => Ne });
	function Ne() {
		return { init() {
			const t = this.$el;
			this.clear && g(t, { "@click"() {
				this.clear();
			} });
			const e = et(t);
			e && (t.style.display = Boolean(e.value) ? "block" : "none", g(e, { "@input"() {
				t.style.display = Boolean(e.value) ? "block" : "none";
			} }), g(t, { "@click"() {
				Z(e, ""), e.dispatchEvent(new Event("cleared", { bubbles: !0 })), e.focus();
			} }));
		} };
	}
	var Re = i({ colorPicker: () => Pe });
	function Pe({ value: t = null, format: e = null } = {}) {
		const i = Fe({ key: "color-picker" });
		return {
			...i,
			value: t,
			format: e ?? "hex",
			init() {
				i.init.call(this);
			},
			hasEyeDropper: () => "undefined" != typeof window && "EyeDropper" in window,
			pick(t) {
				if (this.field.disabled) return;
				const e = t ? S(t, this.format) ?? t : null;
				this.value = e;
			},
			commitTyped(t) {
				if (this.field.disabled) return;
				if (!t) return void this.pick(null);
				const e = S(t, this.format);
				e ? this.pick(e) : this.field.value = this.value ?? "";
			},
			async dropColor() {
				if (this.hasEyeDropper() && !this.field.disabled) try {
					const t = await new window.EyeDropper().open();
					this.pick(t.sRGBHex);
				} catch {}
			},
			clear() {
				this.pick(null);
			}
		};
	}
	var Ue = i({ combobox: () => We });
	function We({ value: t = null, multiple: e = !1 } = {}) {
		const i = $t({
			mode: "manual",
			position: "bottom",
			align: "start",
			matchTriggerWidth: !0
		}), s = Ce({
			hideEmpty: !1,
			clearOnSelect: !e
		}), n = Fe({
			key: "combobox-field",
			serialize() {
				return this.valueString();
			},
			deserialize: (t) => e ? t ? t.split(",").filter(Boolean) : [] : t
		});
		return {
			...i,
			...s,
			...n,
			value: t ?? (e ? [] : null),
			combobox: null,
			selectedLabel() {
				if (e || null == this.value) return null;
				const t = this.items.find((t) => String(this.getElementValue(t.el)) === String(this.value));
				return t ? t.el.querySelector("[data-item-content]")?.textContent?.trim() : null;
			},
			selectedCount() {
				return this.items.filter((t) => this.isSelected(this.getElementValue(t.el))).length;
			},
			valueString() {
				return e ? (this.value ?? []).join(",") : this.value ?? null;
			},
			init() {
				i.init.call(this), s.init.call(this), this.combobox = this.$root.querySelector(K("combobox")), n.init.call(this), g(this.combobox, {
					"@click"() {
						this.combobox.focus(), this.toggle();
					},
					"@keydown.enter.prevent"() {
						if (!this.opened) return this.open();
						this.select(this.index);
					},
					"@keydown.space.prevent"() {
						if (!this.opened) return this.open();
						this.select(this.index);
					},
					"@keydown.arrow-up.prevent"() {
						if (!this.opened) return this.open();
						this.lastInteraction = "keyboard", this.prev();
					},
					"@keydown.arrow-down.prevent"() {
						if (!this.opened) return this.open();
						this.lastInteraction = "keyboard", this.next();
					}
				}), g([
					this.combobox,
					this.popoverElement,
					this.input,
					this.list
				], { "@keydown.escape.prevent"() {
					this.closeAndFocus();
				} }), g(this.$root, {
					"@click.outside"() {
						this.close();
					},
					"@listbox-item-selected"({ detail: t }) {
						this.pick(this.getElementValue(t.button));
					},
					"@listbox-items-changed"() {
						this.syncChecked();
					}
				}), this.$watch("value", () => this.syncChecked()), this.$nextTick(() => this.syncChecked());
			},
			open() {
				i.open.call(this, !1);
				const t = e ? this.value.at(-1) : this.value, s = this.filteredItems.findIndex((e) => String(this.getElementValue(e.el)) === String(t));
				this.index = -1 === s ? null : s, requestAnimationFrame(() => {
					this.input?.focus();
				});
			},
			close() {
				i.close.call(this), this.clear();
			},
			closeAndFocus() {
				this.close(), this.combobox.focus();
			},
			isSelected(t) {
				return e ? (Array.isArray(this.value) || null == this.value || (this.value = [this.value]), this.value.map(String).includes(String(t))) : String(this.value ?? "") === String(t);
			},
			pick(t) {
				e ? this.value = this.isSelected(t) ? this.value.filter((e) => String(e) !== String(t)) : [...this.value, t] : (this.value = this.isSelected(t) ? null : t, this.closeAndFocus());
			},
			remove(t) {
				e && (this.value = this.value.filter((e) => String(e) !== String(t)));
			},
			clearValue() {
				this.value = e ? [] : null;
			},
			syncChecked() {
				this.items.forEach((t) => {
					const e = this.isSelected(this.getElementValue(t.el)), i = t.el.querySelector(K("checkmark"));
					i && i.classList.toggle("invisible", !e), t.li.setAttribute("aria-selected", String(e));
				});
			},
			getElementValue: (t) => t.getAttribute("value") ?? t.textContent?.trim() ?? null
		};
	}
	var He = i({ composer: () => Ve });
	function Ve({ submit: t = !1, placeholder: e = !1 } = {}) {
		const i = Fe({ key: "composer" });
		return {
			...i,
			value: null,
			init() {
				i.init.call(this);
				const s = t ? Array.isArray(t) ? t : [t] : [], n = tt(this.$el.parentElement, "label")?.getAttribute("for") ?? null;
				g(this.$el.querySelector(K("control")), {
					"x-model": "value",
					...n && { id: n },
					...e && { placeholder: e },
					...s.length && { "@keydown"(t) {
						s.some((e) => {
							switch (e) {
								case "enter": return "Enter" === t.key && !t.shiftKey && !t.ctrlKey && !t.metaKey;
								case "ctrl+enter": return "Enter" === t.key && (t.ctrlKey || t.metaKey);
								default: return !1;
							}
						}) && (t.preventDefault(), this.$root?.closest("form")?.requestSubmit());
					} }
				});
			}
		};
	}
	var Ye = i({ copyable: () => Ke });
	function Ke(t = null, e = null) {
		return {
			copied: !1,
			timeout: null,
			findTarget() {
				if (t) {
					const e = document.getElementById(t);
					if (e) return e;
				}
				const e = K("control");
				return this.$el.closest(K("field-control"))?.querySelector(e) ?? this.$el.previousElementSibling?.querySelector(e) ?? this.$el.parentElement?.previousElementSibling?.querySelector(e) ?? null;
			},
			init() {
				this.findTarget() || e ? navigator.clipboard ? g(this.$el, {
					":aria-pressed"() {
						return this.copied;
					},
					async "@click"() {
						clearTimeout(this.timeout), this.copied = !0, this.$el.dispatchEvent(new CustomEvent("open"));
						const t = e ? null : this.findTarget(), i = e ?? ("value" in t ? t.value : t.innerText);
						await navigator.clipboard.writeText(i), t?.dispatchEvent(new Event("copied", { bubbles: !0 })), this.timeout = setTimeout(() => {
							this.$el.dispatchEvent(new CustomEvent("close")), this.copied = !1, this.timeout = null;
						}, 1e3);
					}
				}) : this.$el.disabled = !0 : this.$el.remove();
			},
			destroy() {
				clearTimeout(this.timeout);
			}
		};
	}
	var Xe = i({ creditCard: () => Je });
	function Je(t = {}, e = {}) {
		const i = kt();
		return {
			...i,
			types: t,
			options: {
				opened: !0,
				holderName: null,
				number: null,
				type: null,
				expirationDate: null,
				cvv: null,
				...e
			},
			init() {
				i.init.call(this), this.opened = this.options.opened, g(this.$el, {
					"@click"() {
						this.toggle();
					},
					"@keydown.enter.prevent"() {
						this.toggle();
					},
					"@keydown.space.prevent"() {
						this.toggle();
					},
					":class"() {
						return { "rotate-y-180": !this.isOpened() };
					}
				});
			},
			typeOptions() {
				return this.types[this.options.type] ? this.types[this.options.type] : this.types.unknown;
			},
			update(t = {}) {
				this.options = {
					...this.options,
					...t
				}, "opened" in t && (this.opened = this.options.opened);
			},
			flip(t = !1) {
				t ? this.close() : this.open();
			}
		};
	}
	var Qe = i({ datePicker: () => ti }), Ze = [
		"full",
		"long",
		"medium",
		"short"
	], Ge = "medium";
	function ti({ mode: t = null, multiple: e = null, format: i = null, labels: s = null, type: n = null, openTo: r = null, forceOpenTo: o = null, withConfirmation: a = null, ...l } = {}) {
		i && !Ze.includes(i) && (console.warn(`[tallkit] tk:date-picker received an invalid "format" ("${i}"). Expected one of: ${Ze.join(", ")}. Falling back to "${Ge}".`), i = Ge);
		const h = $t({
			mode: "dropdown",
			position: "bottom",
			align: "start"
		}), c = _e({
			mode: t,
			multiple: e,
			openTo: r,
			...l
		}), u = Fe({
			key: "date-picker",
			property: "committed",
			serialize() {
				return this.committedString();
			},
			deserialize(t) {
				return this.parseInitialValue(t);
			}
		});
		return {
			...h,
			...c,
			...u,
			committed: null,
			typed: "",
			typing: !1,
			init() {
				h.init.call(this), c.init.call(this), this.committed = this.value, u.init.call(this), JSON.stringify(this.value) !== JSON.stringify(this.committed) && (this.value = this.committed), this.syncTyped(), this.$watch("value", () => {
					this.syncTyped(), a || (this.committed = this.value, e || ("range" !== t || this.value?.start && this.value?.end) && (this.typing || this.close()));
				}), this.$watch("committed", () => {
					JSON.stringify(this.value) !== JSON.stringify(this.committed) && (this.value = this.committed);
				}), this.$watch("typed", () => {
					this.typing && this.commitTyped();
				});
			},
			onOpen() {
				a && (this.value = this.committed), o && r && (this.anchorMonth = F(T(r))), h.onOpen.call(this);
			},
			apply() {
				this.committed = this.value, this.close();
			},
			cancel() {
				this.value = this.committed, this.close();
			},
			setSingleValue(i) {
				"range" === t || e || i && this.isDayDisabled(i) || (this.value = i || null, this.focused = this.value ?? this.focused, this.$dispatch("calendar-picked", { value: this.value }));
			},
			formatted() {
				if (!this.value) return null;
				let n;
				try {
					n = new Intl.DateTimeFormat(this.locale, { dateStyle: i ?? Ge });
				} catch {
					n = new Intl.DateTimeFormat(void 0, { dateStyle: Ge });
				}
				if ("range" === t) {
					if (!this.value.start || !this.value.end) return null;
					const t = T(this.value.start), e = T(this.value.end);
					return n.formatRange ? n.formatRange(t, e) : `${n.format(t)} – ${n.format(e)}`;
				}
				return e ? this.value.length ? `${this.value.length} ${s?.selected ?? "selected"}` : null : n.format(T(this.value));
			},
			committedString() {
				return "range" === t ? this.committed?.start ? this.committed.end ? `${this.committed.start}/${this.committed.end}` : this.committed.start : null : e ? (this.committed ?? []).join(",") : this.committed ?? null;
			},
			typable: () => "input" === n && !e,
			maskPattern() {
				const e = q(this.locale).map((t) => "year" === t ? "9999" : "99").join("/");
				return "range" === t ? `${e} – ${e}` : e;
			},
			requiredDigitCount: () => "range" === t ? 16 : 8,
			syncTyped() {
				this.typable() && (this.typed = this.formattedEditable());
			},
			formattedEditable() {
				if ("range" === t) {
					const t = this.value?.start ? N(this.value.start, this.locale) : "", e = this.value?.end ? N(this.value.end, this.locale) : "";
					return t || e ? `${t} – ${e}` : "";
				}
				return this.value ? N(this.value, this.locale) : "";
			},
			commitTyped() {
				if (this.typable() && !((this.typed.match(/\d/g) ?? []).length < this.requiredDigitCount())) if ("range" === t) {
					const [t, e] = this.typed.split(/\s*[–—]\s*/), i = R(t, this.locale), s = R(e, this.locale);
					i && (this.setRangeBound("start", i), this.anchorMonth = F(T(i))), s && (this.setRangeBound("end", s), this.anchorMonth = F(T(s)));
				} else {
					const t = R(this.typed, this.locale);
					t && !this.isDayDisabled(t) && (this.value = t, this.focused = t, this.anchorMonth = F(T(t)), this.$dispatch("calendar-picked", { value: t }));
				}
			},
			confirmTyped() {
				this.commitTyped(), this.typing = !1, this.syncTyped(), a || this.close();
			},
			onFieldBlur(t) {
				this.$root.contains(t.relatedTarget) ? (this.commitTyped(), this.typing = !1, this.syncTyped()) : this.confirmTyped();
			},
			presetRange(e) {
				if ("range" !== t) return null;
				const i = M(/* @__PURE__ */ new Date()), s = T(i);
				switch (e) {
					case "today": return {
						start: i,
						end: i
					};
					case "yesterday": {
						const t = B(i, -1);
						return {
							start: t,
							end: t
						};
					}
					case "thisWeek": return {
						start: L(s, this.startDay),
						end: i
					};
					case "last7Days": return {
						start: B(i, -6),
						end: i
					};
					case "last14Days": return {
						start: B(i, -13),
						end: i
					};
					case "last30Days": return {
						start: B(i, -29),
						end: i
					};
					case "thisMonth": return {
						start: M(F(s)),
						end: M(_(s))
					};
					case "lastMonth": {
						const t = I(s, -1);
						return {
							start: M(F(t)),
							end: M(_(t))
						};
					}
					case "thisYear": return {
						start: `${s.getFullYear()}-01-01`,
						end: `${s.getFullYear()}-12-31`
					};
					case "lastYear": return {
						start: s.getFullYear() - 1 + "-01-01",
						end: s.getFullYear() - 1 + "-12-31"
					};
					default: return null;
				}
			},
			isPresetActive(t) {
				const e = this.presetRange(t);
				return !!e && this.value?.start === e.start && this.value?.end === e.end;
			},
			applyPreset(t) {
				const e = this.presetRange(t);
				e && (this.value = e, this.focused = e.end, this.$dispatch("calendar-picked", { value: e }));
			}
		};
	}
	var ei = i({ disclosureGroup: () => ii });
	function ii({ exclusive: t = !1 } = {}) {
		return {
			observer: null,
			init() {
				const e = () => Array.from(this.$root.querySelectorAll(K("disclosure-item"))), i = () => {
					e().forEach((t) => {
						this.observer.observe(t, { attributeFilter: ["data-open"] });
					});
				};
				this.observer = new MutationObserver((s) => {
					const n = e();
					if (t) {
						const t = new Set(s.filter((t) => t.target.hasAttribute("data-open")).map((t) => t.target));
						n.forEach((e) => {
							t.has(e) || e.removeAttribute("data-open");
						});
					}
					this.observer.disconnect(), this.$dispatch("changed", { items: n }), this.$nextTick(i);
				}), i();
			},
			destroy() {
				this.observer?.disconnect();
			}
		};
	}
	var si = i({ disclosure: () => ni });
	function ni() {
		const t = kt();
		return {
			...t,
			observer: null,
			init() {
				t.init.call(this, this.$root.hasAttribute("data-open"));
				const e = this.$root.querySelector(":scope > button + *");
				e && !e.id && (e.id = J("disclosure")), this.observer = new MutationObserver(() => {
					this.opened = this.$root.hasAttribute("data-open");
				}), this.observer.observe(this.$root, { attributeFilter: ["data-open"] }), g(this.$root.querySelectorAll(":scope > button"), {
					":aria-controls": () => e?.id ?? null,
					":aria-expanded"() {
						return String(this.opened);
					},
					"@click"() {
						this.toggle();
					}
				});
			},
			open() {
				this.$root.setAttribute("data-open", ""), t.open.call(this);
			},
			close() {
				this.$root.removeAttribute("data-open"), t.close.call(this);
			},
			destroy() {
				this.observer?.disconnect();
			}
		};
	}
	var ri = i({ echarts: () => oi });
	function oi() {
		const t = gt();
		return {
			...t,
			...pt(),
			chart: null,
			init() {
				this.load(() => c(() => !!window.echarts, "https://cdn.jsdelivr.net/npm/echarts@6"));
			},
			render(t = {}) {
				try {
					this.chart ??= window.echarts.init(this.$refs.target), this.chart.setOption({
						...t,
						...this.getDataOptions(this.$refs.target)
					}), this.$dispatch("rendered", { chart: this.chart });
				} catch (t) {
					this.fail(t);
				}
			},
			destroy() {
				t.destroy.call(this), this.chart?.dispose(), this.chart = null;
			}
		};
	}
	var ai = [
		"text",
		"heading",
		"color",
		"size",
		"script",
		"align",
		"link",
		"list",
		"media",
		"table",
		"quote",
		"code"
	];
	function li(t, e) {
		const i = (t ?? "").trim().split(/\s+/).filter(Boolean);
		return i.length ? i.includes("none") ? [] : (i.filter((t) => "full" !== t && !e.includes(t)).forEach((t) => console.warn(`[tallkit] Unknown editor mode group "${t}"`)), i.includes("full") ? e : e.filter((t) => i.includes(t))) : null;
	}
	function hi() {
		return {
			input: null,
			_lastSynced: null,
			initField() {
				if (this.input = this.$root.querySelector(K("control")), this.$wire) {
					const t = rt(this.input);
					t && this.$wire.$watch(t.name, (t) => {
						t !== this._lastSynced && this.isCompleted() && this.applyExternalValue(t);
					});
				}
			},
			sync(t) {
				this._lastSynced = t, Z(this.input, t);
			}
		};
	}
	var ci = i({ editorjs: () => di }), ui = {
		text: {
			scripts: ["https://cdn.jsdelivr.net/npm/@editorjs/inline-code@1", "https://cdn.jsdelivr.net/npm/@editorjs/underline@1"],
			inline: [
				"bold",
				"italic",
				"underline",
				"inlineCode"
			],
			tools: () => ({
				inlineCode: window.InlineCode,
				underline: window.Underline
			})
		},
		heading: {
			scripts: ["https://cdn.jsdelivr.net/npm/@editorjs/header@2"],
			tools: () => ({ heading: window.Header })
		},
		color: {
			scripts: ["https://cdn.jsdelivr.net/npm/@editorjs/marker@1"],
			inline: ["marker"],
			tools: () => ({ marker: window.Marker })
		},
		link: {
			scripts: [],
			inline: ["link"],
			tools: () => ({})
		},
		list: {
			scripts: ["https://cdn.jsdelivr.net/npm/@editorjs/list@2"],
			tools: () => ({ list: {
				class: window.EditorjsList,
				inlineToolbar: !0
			} })
		},
		media: {
			scripts: ["https://cdn.jsdelivr.net/npm/@editorjs/simple-image@1", "https://cdn.jsdelivr.net/npm/@editorjs/embed@2"],
			tools: () => ({
				simpleImage: window.SimpleImage,
				embed: window.Embed
			})
		},
		table: {
			scripts: ["https://cdn.jsdelivr.net/npm/@editorjs/table@2"],
			tools: () => ({ table: window.Table })
		},
		quote: {
			scripts: [
				"https://cdn.jsdelivr.net/npm/@editorjs/quote@2",
				"https://cdn.jsdelivr.net/npm/@editorjs/warning@1",
				"https://cdn.jsdelivr.net/npm/@editorjs/delimiter@1"
			],
			tools: () => ({
				quote: {
					class: window.Quote,
					inlineToolbar: !0
				},
				warning: window.Warning,
				delimiter: window.Delimiter
			})
		},
		code: {
			scripts: ["https://cdn.jsdelivr.net/npm/@editorjs/code@2", "https://cdn.jsdelivr.net/npm/@editorjs/raw@2"],
			tools: () => ({
				code: window.CodeTool,
				raw: window.RawTool
			})
		}
	};
	function di({ options: t = {}, scripts: e = [], styles: i = [], mode: s = null } = {}) {
		const n = gt();
		return {
			...n,
			...pt(),
			...hi(),
			editor: null,
			_saveToken: 0,
			init() {
				this.initField();
				const t = li(s, ai) ?? ai;
				this.load(() => c(() => !!window.EditorJS, [
					"https://cdn.jsdelivr.net/npm/@editorjs/editorjs@2",
					...t.flatMap((t) => ui[t]?.scripts ?? []),
					...e
				], i).then(() => this.mount(t)));
			},
			applyExternalValue(t) {
				this.editor.render(t ? JSON.parse(t) : { blocks: [] });
			},
			mount(e) {
				try {
					this.editor = new window.EditorJS({
						holder: this.$refs.root,
						tools: e.reduce((t, e) => ({
							...t,
							...ui[e]?.tools()
						}), {}),
						inlineToolbar: e.flatMap((t) => ui[t]?.inline ?? []),
						data: this.input.value ? JSON.parse(this.input.value) : void 0,
						onChange: async (t) => {
							const e = ++this._saveToken, i = await t.saver.save();
							e === this._saveToken && this.sync(JSON.stringify(i));
						},
						...t,
						...this.getDataOptions(this.$refs.root)
					}), this.editor.isReady.then(() => this.$dispatch("rendered", { editor: this.editor }));
				} catch (t) {
					this.fail(t);
				}
			},
			async destroy() {
				n.destroy.call(this), await this.editor?.destroy(), this.editor = null;
			}
		};
	}
	var pi = i({ fetchable: () => mi });
	function mi({ url: t = null, data: e = null, auto: i = null, options: s = {} } = {}) {
		const n = gt();
		return {
			...n,
			url: null,
			response: null,
			data: null,
			options: null,
			_controller: null,
			init() {
				this.clear(), this.url = t, this.data = e, this.options = {
					method: "get",
					headers: { Accept: "application/json" },
					responseType: "json",
					...s
				}, this.url && !1 !== i && this.fetch(), !this.url && this.data && this.complete();
			},
			async fetch(t = null, e = {}, i = !1) {
				const s = t || this.url, n = {
					...this.options ?? {},
					...e,
					headers: {
						...this.options?.headers ?? {},
						...e.headers ?? {}
					}
				};
				if (this.url = s, this.options = n, !s) return;
				this._controller?.abort();
				const r = new AbortController();
				this._controller = r, this.load(async () => {
					if (this.response = await window.fetch(s, {
						...n,
						signal: r.signal
					}), !this.response.ok) throw new Error(this.response.statusText);
					this.data = n.responseType ? await this.response[n.responseType]() : this.response;
				}, i);
			},
			reload() {
				return this.fetch();
			},
			update(t = null, e = {}) {
				return this.fetch(t, e, !0);
			},
			destroy() {
				n.destroy.call(this), this._controller?.abort(), this._controller = null;
			}
		};
	}
	var gi = i({ form: () => fi });
	function fi({ action: t = null, focusError: e = null, clearErrorsOnSubmit: i = null, toast: s = null, errorMessage: n = null, successMessage: r = null } = {}) {
		return {
			livewireCommitCleanup: null,
			init() {
				st() ? this.watchLivewireCommits() : e && this.focusFirstInvalidField();
			},
			watchLivewireCommits() {
				this.livewireCommitCleanup = nt(({ component: o, commit: a, succeed: l }) => {
					(o?.el === this.$el || o?.el?.contains(this.$el)) && (t && !a?.calls?.some((e) => e.method === t) || (i && this.clearErrors(), l(({ snapshot: t }) => {
						if (!this.$el?.isConnected) return;
						const i = this.$el?.getAttribute("id") ?? o?.el.getAttribute("wire:id") ?? void 0;
						if (Object.keys(t?.memo?.errors ?? {}).length > 0 || this.$el.querySelector("[data-invalid], [aria-invalid=\"true\"]")) return !0 !== s && "error" !== s || !n || this.$tallkit.toast().error({
							message: n,
							id: i,
							duration: 3e3
						}), void (e && this.focusFirstInvalidField());
						!0 !== s && "success" !== s || !r || this.$tallkit.toast().success({
							message: r,
							id: i,
							duration: 3e3
						});
					})));
				});
			},
			clearErrors() {
				this.$el.querySelectorAll("[data-invalid], [aria-invalid=\"true\"]").forEach((t) => {
					t.removeAttribute("data-invalid"), t.removeAttribute("aria-invalid");
				}), this.$el.querySelectorAll("[data-tallkit-error], [data-tallkit-error-group]").forEach((t) => t.remove());
			},
			focusFirstInvalidField() {
				const t = this.$el.querySelector("[data-invalid], [aria-invalid=\"true\"]");
				t instanceof HTMLElement && (t.scrollIntoView({
					behavior: "smooth",
					block: "center"
				}), t.focus({ preventScroll: !0 }));
			},
			destroy() {
				this.livewireCommitCleanup?.();
			}
		};
	}
	var vi = i({ frappeCharts: () => yi });
	function yi() {
		const t = gt();
		return {
			...t,
			...pt(),
			chart: null,
			init() {
				this.load(() => c(() => !!window.frappe?.Chart, "https://cdn.jsdelivr.net/npm/frappe-charts@1"));
			},
			render(t = {}) {
				try {
					this.chart?.destroy?.(), this.chart = new window.frappe.Chart(this.$refs.target, {
						...t,
						...this.getDataOptions(this.$refs.target)
					}), this.$dispatch("rendered", { chart: this.chart });
				} catch (t) {
					this.fail(t);
				}
			},
			destroy() {
				t.destroy.call(this), this.chart?.destroy?.(), this.chart = null;
			}
		};
	}
	var wi = i({ fullCalendar: () => bi });
	function bi({ locale: t = null, theme: e = null, palette: i = null, options: s = {} } = {}) {
		const n = gt();
		return {
			...n,
			...pt(),
			fullCalendar: null,
			init() {
				const s = "https://cdn.jsdelivr.net/npm/fullcalendar@7", n = [`${s}/all/global.min.js`];
				t && "en" !== t && n.push(`${s}/locales/${String(t).replace("_", "-").toLowerCase()}/global.min.js`), n.push(`${s}/themes/${e ?? "monarch"}/global.js`), this.load(() => c(() => !!window.FullCalendar, n, [
					`${s}/skeleton.css`,
					`${s}/themes/${e ?? "monarch"}/theme.css`,
					`${s}/themes/${e ?? "monarch"}/palettes/${i ?? "blue"}.css`
				]));
			},
			render() {
				try {
					this.fullCalendar?.destroy(), this.fullCalendar = new window.FullCalendar.Calendar(this.$el, {
						locale: t,
						...s,
						...this.getDataOptions()
					}), this.fullCalendar.render(), this.$dispatch("rendered", { fullCalendar: this.fullCalendar });
				} catch (t) {
					this.fail(t);
				}
			},
			destroy() {
				n.destroy.call(this), this.fullCalendar?.destroy(), this.fullCalendar = null;
			}
		};
	}
	var ki = i({ header: () => Ai });
	function Ai() {
		return { ...yt() };
	}
	var $i = i({ highlightjs: () => xi });
	function xi() {
		return {
			...gt(),
			language: null,
			init() {
				this.load(() => c(() => !!window.hljs, "https://cdn.jsdelivr.net/gh/highlightjs/cdn-release@11/build/highlight.min.js", "https://cdn.jsdelivr.net/gh/highlightjs/cdn-release@11/build/styles/default.min.css"));
			},
			render(t, e = null) {
				try {
					const i = e ? window.hljs.highlight(t, { language: e }) : window.hljs.highlightAuto(t);
					return this.language = i.language ?? null, i.value;
				} catch (e) {
					return X(t) ?? "";
				}
			}
		};
	}
	var Ei = i({ inputViewable: () => Ci });
	function Ci() {
		return {
			viewed: !1,
			inputObserver: null,
			originalType: "password",
			init() {
				const t = et(this.$el);
				t && (t.type && (this.originalType = t.type), t.setAttribute("type", this.viewed ? "text" : this.originalType), g(this.$el, {
					":aria-pressed"() {
						return this.viewed;
					},
					"@click"() {
						this.viewed = !this.viewed, t.setAttribute("type", this.viewed ? "text" : this.originalType), t.dispatchEvent(new Event("viewed", { bubbles: !0 }));
					}
				}), this.inputObserver = new MutationObserver(() => {
					this.viewed = "password" !== t?.getAttribute("type");
				}), this.inputObserver.observe(t, {
					attributes: !0,
					attributeFilter: ["type"]
				}));
			},
			destroy() {
				this.inputObserver?.disconnect();
			}
		};
	}
	var Si = i({ label: () => Di });
	function Di() {
		return { init() {
			if ("label" === this.$el.tagName.toLowerCase() && this.$el.hasAttribute("for") && document.getElementById(this.$el.getAttribute("for"))) return;
			let t = tt(this.$el.parentElement, "control");
			t && !t.matches("input, select, textarea, [contenteditable=\"\"], [contenteditable=\"true\"], [role=\"textbox\"]") && (t = t.querySelector("input, select, textarea, [contenteditable=\"\"], [contenteditable=\"true\"], [role=\"textbox\"]")), t && g(this.$el, { "@click"() {
				const e = t.tagName.toLowerCase(), i = t.getAttribute("type")?.toLowerCase(), s = t.hasAttribute("contenteditable") || "textbox" === t.getAttribute("role"), n = t.hasAttribute("readonly") || "true" === t.getAttribute("aria-readonly"), r = t.disabled;
				"checkbox" !== i ? "radio" !== i ? !s && ![
					"input",
					"select",
					"textarea"
				].includes(e) || "function" != typeof t.focus || r || t.focus() : r || n || t.checked || G(t, !0) : r || n || G(t, !t.checked);
			} });
		} };
	}
	function Mi(t, e) {
		return {
			checked: t,
			value: void 0,
			isControlled() {
				return void 0 !== this.value;
			},
			isArray() {
				return "checkbox" === e && Array.isArray(this.value);
			},
			isChecked() {
				return this.isControlled() ? this.isArray() ? this.value.some((t) => t == this.$root.value) : this.value == this.$root.value : this.checked;
			},
			init() {
				g(this.$el, {
					"@click": () => this.toggle(),
					":data-checked": () => this.isChecked(),
					":aria-checked": () => this.isChecked()
				});
			},
			toggle() {
				this.isControlled() ? this.isArray() ? this.value = this.isChecked() ? this.value.filter((t) => t != this.$root.value) : [...this.value, this.$root.value] : this.value = "radio" !== e && this.isChecked() ? null : this.$root.value : this.checked = !this.checked;
			}
		};
	}
	var Ti = i({ menuCheckbox: () => Fi });
	function Fi(t) {
		return Mi(t, "checkbox");
	}
	var Ii = i({ menuRadio: () => _i });
	function _i(t) {
		return Mi(t, "radio");
	}
	var Li = i({ menu: () => Bi });
	function Bi() {
		return {
			init() {
				const t = Array.from(this.$el.querySelectorAll(K("menu-item"))).filter((t) => t.closest(K("menu")) === this.$el);
				g(t, {
					"@mouseenter"() {
						this.$el.disabled || this.$el.setAttribute("data-active", "");
					},
					"@mouseleave"() {
						this.$el.disabled || this.$el.removeAttribute("data-active");
					},
					"@focus"() {
						this.$el.disabled || this.$el.setAttribute("data-active", "");
					},
					"@blur"() {
						this.$el.removeAttribute("data-active");
					}
				}), g(this.$el, {
					"@keydown.arrow-down.prevent"() {
						this.focusItem(t, 1);
					},
					"@keydown.arrow-up.prevent"() {
						this.focusItem(t, -1);
					},
					"@keydown.home.prevent"() {
						this.focusItem(t, "first");
					},
					"@keydown.end.prevent"() {
						this.focusItem(t, "last");
					}
				});
			},
			focusItem(t, e) {
				const i = t.filter((t) => !t.disabled);
				if (!i.length) return;
				const s = i.indexOf(document.activeElement);
				let n;
				n = "first" === e ? 0 : "last" === e ? i.length - 1 : -1 === s ? 1 === e ? 0 : i.length - 1 : (s + e + i.length) % i.length, i[n].focus();
			}
		};
	}
	var Oi = i({ modalTrigger: () => ji });
	function ji({ name: t = null, shortcut: e = null } = {}) {
		return { init() {
			g(this.$el, { "@click"() {
				this.$el.querySelector("button[disabled]") || this.$dispatch("modal-show", { name: t });
			} }), e && f(this.$el, e, () => this.$dispatch("modal-show", { name: t }));
		} };
	}
	var zi = i({ modal: () => qi });
	function qi({ name: t = null, dismissible: e = null, persist: i = null, shortcut: s = null } = {}) {
		return {
			init() {
				const n = this.$el;
				g(n, {
					"@modal-show.document"(e) {
						(e.detail.name !== t || e.detail.scope) && (e.detail.name !== t || e.detail.scope !== this.$wire?.id) || n.showModal();
					},
					"@modal-close.document"(e) {
						e.detail.name && (e.detail.name !== t || e.detail.scope) && (e.detail.name !== t || e.detail.scope !== this.$wire?.id) || n.close();
					}
				});
				const r = (t, s = !0) => {
					if (t.preventDefault(), i) {
						const t = "string" == typeof i ? i : "tilt-shaking";
						n.classList.remove(t), n.focus(), this.$nextTick(() => n.classList.add(t));
						return;
					}
					if (!1 === e) return;
					const r = t.target;
					s && r !== n && "0" !== r.getAttribute("tabindex") || n.close();
				};
				g(n, {
					"@toggle"(t) {
						"open" === t.newState && (n.querySelector("[tabindex=\"0\"]")?.focus(), this.$dispatch("opened", t)), "closed" === t.newState && this.$dispatch("closed", t);
					},
					"@click"(t) {
						t.target.closest(`${K("modal-close")},${K("modal-auto-close")}`) ? n.close() : r(t);
					},
					"@keydown.escape.prevent"(t) {
						r(t, !1);
					}
				}), s && f(n, s, () => this.$dispatch("modal-show", { name: t }));
			},
			show() {
				this.$dispatch("modal-show", { name: t });
			},
			close() {
				this.$dispatch("modal-close", { name: t });
			}
		};
	}
	var Ni = i({ navIndicator: () => Ri });
	function Ri({ mode: t = null } = {}) {
		return {
			_visibilityTimeout: null,
			init() {
				this._onMove = this.move.bind(this), document.addEventListener("livewire:navigated", this._onMove), window.addEventListener("resize", this._onMove), this.$nextTick(() => this.move());
			},
			destroy() {
				document.removeEventListener("livewire:navigated", this._onMove), window.removeEventListener("resize", this._onMove), clearTimeout(this._visibilityTimeout);
			},
			findNav(t) {
				let e = t;
				for (; e;) {
					const t = e.previousElementSibling;
					if (t?.matches(K("nav"))) return t;
					e = e.parentElement;
				}
				return null;
			},
			move() {
				requestAnimationFrame(() => {
					const e = this.$el, i = this.findNav(e), s = i?.querySelector("a[data-current]");
					if (!s) return;
					const r = e.getBoundingClientRect(), o = s.getBoundingClientRect(), a = s.offsetLeft + i.offsetLeft, l = s.offsetTop + i.offsetTop;
					if (o.width <= 0 || o.height <= 0) return void (e.style.opacity = "0");
					if (e.style.opacity = "1", (r.width <= 0 || r.height <= 0 || r.top <= 0 || r.left <= 0) && (e.style.visibility = "hidden", clearTimeout(this._visibilityTimeout), this._visibilityTimeout = setTimeout(() => {
						e.style.visibility = "visible", this._visibilityTimeout = null;
					}, n(e))), "line-left" === t || "line-right" === t) return e.style.height = `${o.height}px`, void (e.style.transform = `translate(${a + ("line-left" === t ? -10 : o.width + 10)}px, ${l}px)`);
					if ("line-top" === t || "line-bottom" === t) return e.style.width = `${o.width}px`, void (e.style.transform = `translate(${a}px, ${l + ("line-top" === t ? -10 : o.height + 10)}px)`);
					const h = getComputedStyle(s);
					e.style.transform = `translate(${a}px, ${l}px)`, e.style.width = `${o.width}px`, e.style.height = `${o.height}px`, e.style.borderRadius = h.borderRadius;
				});
			}
		};
	}
	var Pi = i({ notificationItem: () => Ui });
	function Ui() {
		return { ...ct("collapse") };
	}
	var Wi = i({ notification: () => Hi });
	function Hi({ channel: t = null } = {}) {
		return {
			init() {
				g(this.$el.querySelectorAll(K("notification-mark-all")), { "@click"(t) {
					(t.currentTarget.closest("[role=tabpanel]") ?? this.$el).querySelectorAll(K("notification-item")).forEach((t) => t.dispatchEvent(new CustomEvent("dismiss")));
				} }), t && window.Echo && this.$wire && window.Echo.private(t).notification(() => {
					this.$wire.$refresh();
				});
			},
			destroy() {
				t && window.Echo && window.Echo.leave(t);
			}
		};
	}
	var Vi = i({ otp: () => Yi });
	function Yi(t) {
		const e = Fe({
			key: "otp-field",
			deserialize: (t) => t || ""
		});
		return {
			...e,
			value: "",
			inputs: [],
			_syncing: !1,
			init() {
				this.inputs = Array.from(this.$root.querySelectorAll("input[data-mode]")), e.init.call(this), this.$nextTick(() => {
					this.syncFromModel(), this.updateModel();
				}), this.$watch("value", (t) => {
					this.syncFromModel(t), this.updateModel();
				}), this.inputs.forEach((t, e) => {
					g(t, this.bindings(t, e, this.inputs));
				});
			},
			bindings(t, e, i) {
				return {
					"@focus": () => this.handleFocus(t, e, i),
					"@blur": () => this.$dispatch("otp-blur", {
						input: t,
						index: e
					}),
					"@paste.prevent": (t) => this.handlePaste(t, e, i),
					"@input": () => this.handleInput(t, e, i),
					"@keydown": (s) => this.handleKeydown(s, t, e, i),
					"@keydown.arrow-left.prevent": () => i[e - 1]?.select(),
					"@keydown.arrow-right.prevent": () => i[e + 1]?.select(),
					"@keydown.backspace.prevent": () => this.handleBackspace(t, e, i)
				};
			},
			handleFocus(t, e, i) {
				if (t.value) return t.select(), void this.$dispatch("otp-focus", {
					input: t,
					index: e
				});
				const s = i.find((t) => !t.value);
				s?.select(), this.$dispatch("otp-focus", {
					input: s || t,
					index: i.indexOf(s || t)
				});
			},
			handlePaste(t, e, i) {
				const s = t.clipboardData?.getData("text") ?? "";
				this._syncing = !0;
				try {
					Xi(s, e, i);
				} finally {
					this._syncing = !1;
				}
				this.updateModel(), this.$dispatch("otp-paste", {
					pasted: s,
					index: e
				});
			},
			handleInput(t, e, i) {
				if (this._syncing) return;
				const s = t.dataset.mode, n = Ki(t.value, s);
				n.length > 1 ? Xi(n, e, i) : (t.value = n, n && i[e + 1]?.focus()), this.updateModel();
			},
			handleKeydown(t, e, i, s) {
				if (t.ctrlKey || t.metaKey || t.altKey) return;
				const n = e.dataset.mode;
				(function(t, e) {
					return !![
						"Backspace",
						"Delete",
						"Tab",
						"ArrowLeft",
						"ArrowRight"
					].includes(t) || Ki(t, e).length > 0;
				})(t.key, n) || t.preventDefault();
			},
			handleBackspace(t, e, i) {
				t.value ? (this._syncing = !0, Z(t, ""), this._syncing = !1) : i[e - 1]?.select(), this.updateModel();
			},
			syncFromModel(t) {
				t ??= this.value;
				const e = String(t).padEnd(this.inputs.length).split("");
				this._syncing = !0;
				try {
					this.inputs.forEach((t, i) => {
						const s = t.dataset.mode;
						Z(t, Ki(e[i] ?? "", s));
					});
				} finally {
					this._syncing = !1;
				}
			},
			updateModel() {
				const e = this.inputs.map((t) => t.value || "");
				this.value = e.join("");
				const i = e.filter(Boolean).length;
				this.$dispatch("otp-change", { value: this.value }), i === this.inputs.length ? (this.$dispatch("otp-complete", { value: this.value }), "auto" === t ? this.$root.closest("form")?.requestSubmit() : t && st() && window.Livewire.dispatch(t, this.value)) : this.$dispatch("otp-incomplete", { value: this.value }), 0 === i && this.$dispatch("otp-clear");
			}
		};
	}
	function Ki(t, e = "numeric") {
		return (t.toUpperCase().match({
			numeric: /[0-9]/g,
			alpha: /[A-Z]/g,
			alphanumeric: /[A-Z0-9]/g
		}[e]) || []).join("");
	}
	function Xi(t, e, i) {
		const s = t.split("");
		s.forEach((t, s) => {
			const n = i[e + s];
			if (!n) return;
			Z(n, Ki(t, n.dataset.mode));
		}), i[Math.min(e + s.length, i.length - 1)]?.focus();
	}
	var Ji = i({ prettyPrintJson: () => Qi });
	function Qi() {
		return {
			...gt(),
			init() {
				this.load(() => c(() => !!window.prettyPrintJson, "https://cdn.jsdelivr.net/npm/pretty-print-json@3/dist/pretty-print-json.min.js", "https://cdn.jsdelivr.net/npm/pretty-print-json@3/dist/css/pretty-print-json.min.css"));
			},
			render(t = null, e = null) {
				try {
					return "string" == typeof t && (t = JSON.parse(t)), window.prettyPrintJson.toHtml(t, e || {});
				} catch (e) {
					return X("string" == typeof t ? t : JSON.stringify(t, null, 2)) ?? "";
				}
			}
		};
	}
	var Zi = i({ progress: () => Gi });
	function Gi(t = null) {
		return {
			value: 0,
			init() {
				this.updateValue(t ?? 0);
			},
			updateValue(t) {
				const e = Number(t);
				Number.isNaN(e) || (this.value = Math.max(0, Math.min(100, e)));
			}
		};
	}
	var ts = i({ quill: () => ss }), es = {
		text: [[
			"bold",
			"italic",
			"underline",
			"strike"
		]],
		heading: [[{ header: [
			1,
			2,
			3,
			4,
			5,
			6,
			!1
		] }]],
		color: [[{ color: [] }, { background: [] }]],
		size: [[{ size: [
			"small",
			!1,
			"large",
			"huge"
		] }]],
		script: [[{ script: "sub" }, { script: "super" }]],
		align: [
			[{ align: [] }],
			[{ indent: "-1" }, { indent: "+1" }],
			[{ direction: "rtl" }]
		],
		link: [["link", "formula"]],
		list: [[
			{ list: "ordered" },
			{ list: "bullet" },
			{ list: "check" }
		]],
		media: [["image", "video"]],
		quote: [["blockquote"]],
		code: [["code-block"]]
	};
	function is(t) {
		const e = li(t, ai) ?? ai;
		return !!e.length && [...e.flatMap((t) => es[t] ?? []), ["clean"]];
	}
	function ss({ options: t = {}, scripts: e = [], styles: i = [], mode: s = null } = {}) {
		const n = gt();
		return {
			...n,
			...pt(),
			...hi(),
			editor: null,
			init() {
				this.initField(), this.load(() => c(() => !!window.Quill && !!window.DOMPurify, [
					"https://cdn.jsdelivr.net/npm/dompurify@3/dist/purify.min.js",
					"https://cdn.jsdelivr.net/npm/quill@2/dist/quill.js",
					...e
				], ["https://cdn.jsdelivr.net/npm/quill@2/dist/quill.snow.css", ...i]).then(() => this.mount()));
			},
			applyExternalValue(t) {
				this.editor.clipboard.dangerouslyPasteHTML(window.DOMPurify.sanitize(t ?? ""));
			},
			mount() {
				try {
					this.editor = new window.Quill(this.$refs.root, {
						theme: "snow",
						modules: { toolbar: is(s) },
						...t,
						...this.getDataOptions(this.$refs.root)
					}), this.editor.on("text-change", () => {
						this.sync(this.editor.root.innerHTML);
					}), this.$dispatch("rendered", { editor: this.editor });
				} catch (t) {
					this.fail(t);
				}
			},
			destroy() {
				n.destroy.call(this), this.editor?.off("text-change"), this.editor = null;
			}
		};
	}
	var ns = i({ sidebar: () => rs });
	function rs(t, e, i) {
		const s = kt(), n = yt();
		return {
			...s,
			...n,
			init() {
				s.init.call(this), e && n.init.call(this), i && (this.$el.removeAttribute("data-mobile-cloak"), this.screenLg = window.innerWidth >= 1024, g(this.$el, {
					":data-stashed"() {
						return !this.screenLg;
					},
					"x-resize.document"() {
						this.screenLg = window.innerWidth >= 1024;
					},
					[`@sidebar-${t ?? ""}-close.window`]() {
						this.close();
					},
					[`@sidebar-${t ?? ""}-toggle.window`]() {
						this.toggle();
					},
					"@keydown.escape.window"() {
						this.isOpened() && this.close();
					}
				}), this._dispatchState());
			},
			open() {
				this.$el.setAttribute("data-show-stashed-sidebar", ""), s.open.call(this), this._dispatchState();
			},
			close() {
				this.$el.removeAttribute("data-show-stashed-sidebar"), s.close.call(this), this._dispatchState();
			},
			_dispatchState() {
				window.dispatchEvent(new CustomEvent(`sidebar-${t ?? ""}-state`, { detail: { opened: this.opened } }));
			},
			destroy() {
				e && n.destroy.call(this);
			}
		};
	}
	var os = i({ slider: () => as });
	function as() {
		return {
			input: null,
			value: null,
			init() {
				if (this.input = this.$root.querySelector(K("control")), this.$nextTick(() => this.updateRange()), this.$wire) {
					const t = rt(this.input);
					t && this.$wire.$watch(t.name, () => this.updateRange());
				}
				g(this.input, { "@input": () => this.updateRange() }), g(this.$root.querySelector(K("slider-ticks")), { "@click": (t) => {
					const e = [...this.$root.querySelectorAll(K("slider-tick"))], i = t.clientX;
					let s = null, n = 1 / 0;
					if (e.forEach((t) => {
						const e = t.getBoundingClientRect(), r = e.left + e.width / 2, o = Math.abs(i - r);
						o < n && (n = o, s = t);
					}), s) {
						let t = parseInt(s.getAttribute("data-value") ?? "");
						isNaN(t) && (t = parseInt(s.textContent?.trim() ?? "")), isNaN(t) || this.setValue(t);
					}
				} });
			},
			setValue(t) {
				this.input.disabled || Z(this.input, t);
			},
			updateRange() {
				const t = Number(this.input.min || 0), e = Number(this.input.max || 100), i = Number(this.input.value), s = e === t ? 0 : 100 * (i - t) / (e - t);
				this.value = this.input.value, this.input.style.setProperty("--range-percent", `${s}%`), this.input.classList.toggle("before:rounded-r-none", s < 50);
			}
		};
	}
	var ls = i({ submenu: () => hs });
	function hs() {
		const t = $t({
			mode: "manual",
			position: W() ? "left" : "right",
			align: "start"
		});
		return {
			...t,
			_i: null,
			inside: !1,
			init() {
				t.init.call(this), g(this.popoverElement, {
					"@mouseenter"() {
						this.inside = !0, this.trigger.setAttribute("data-active", "");
					},
					"@mouseleave"() {
						this.inside = !1, this.timerToClose();
					}
				}), g(this.trigger, {
					"@click"() {
						this.toggle(!1);
					},
					"@mouseenter"() {
						clearTimeout(this._i), this.open(!1);
					},
					"@mouseleave"() {
						this.timerToClose();
					}
				});
			},
			timerToClose() {
				this._i = setTimeout(() => {
					this.inside || (this.close(), this.trigger.removeAttribute("data-active"));
				}, 10);
			},
			destroy() {
				clearTimeout(this._i), t.destroy.call(this);
			}
		};
	}
	var cs = i({ tab: () => us });
	function us({ selectFirst: t = null, orientation: e = null } = {}) {
		return {
			selected: null,
			tabs() {
				return Array.from(this.$root.querySelectorAll("[role=\"tab\"]")).filter((t) => !t.disabled);
			},
			init() {
				const i = this.$root.querySelector("[data-selected]")?.dataset.name, s = this.tabs();
				(i || t && s.length) && this.$nextTick(() => {
					this.select(i ?? s[0]?.dataset.name);
				});
				const n = "vertical" === e ? "arrow-down" : "arrow-right", r = "vertical" === e ? "arrow-up" : "arrow-left";
				g(this.$root, {
					[`@keydown.${n}`](t) {
						t.target.closest("[role=\"tab\"]") && (t.preventDefault(), this.focusTab(1, t.target));
					},
					[`@keydown.${r}`](t) {
						t.target.closest("[role=\"tab\"]") && (t.preventDefault(), this.focusTab(-1, t.target));
					},
					"@keydown.home"(t) {
						t.target.closest("[role=\"tab\"]") && (t.preventDefault(), this.focusTab("first", t.target));
					},
					"@keydown.end"(t) {
						t.target.closest("[role=\"tab\"]") && (t.preventDefault(), this.focusTab("last", t.target));
					}
				});
			},
			isSelected(t) {
				return this.selected === t;
			},
			select(t) {
				this.selected = t;
			},
			focusTab(t, e) {
				const i = this.tabs();
				if (!i.length) return;
				const s = i.indexOf(e);
				let n;
				n = "first" === t ? 0 : "last" === t ? i.length - 1 : (s + t + i.length) % i.length;
				const r = i[n];
				r.focus(), r.dataset.name && this.select(r.dataset.name);
			}
		};
	}
	var ds = i({ table: () => ps });
	function ps() {
		return {
			boundElements: /* @__PURE__ */ new WeakSet(),
			rows: [],
			selected: [],
			selectedIds: [],
			selectAllChecked: !1,
			observer: null,
			init() {
				this.resetSelection();
				const t = this.$el.querySelector("table > tbody");
				t && (this.observer = new MutationObserver(() => this.update()), this.observer.observe(t, {
					childList: !0,
					subtree: !0
				}));
			},
			destroy() {
				this.observer?.disconnect();
			},
			update() {
				const t = this.$el.querySelector("table > tbody"), e = t ? Array.from(t.querySelectorAll(":scope > tr[role=\"row\"]")) : [];
				this.rows = e.map((t) => {
					const e = t.querySelector("[data-role=row-selection]"), i = t.querySelectorAll("[data-role=row-expanded]"), s = {
						el: t,
						id: t.dataset.id,
						selection: e,
						expanded: i
					};
					e && !this.boundElements.has(e) && (this.boundElements.add(e), g(e, { "@click"() {
						this._updateRowState(s), this._syncSelect();
					} }));
					const n = Array.from(i).filter((t) => !this.boundElements.has(t));
					return n.length && (n.forEach((t) => this.boundElements.add(t)), g(n, { "@click"() {
						s.el.dataset.expanded = "open" === s.el.dataset.expanded ? "close" : "open";
					} })), s;
				}), this.rows.forEach((t) => {
					t.selection && G(t.selection, this.selectAllChecked || this.selectedIds.includes(t.id)), this._updateRowState(t);
				}), this._syncSelect();
			},
			toggleAll() {
				this.rows.forEach((t) => {
					t.selection && (G(t.selection, this.selectAllChecked), this._updateRowState(t));
				}), this._syncSelect();
			},
			resetSelection() {
				this.selected = [], this.selectedIds = [], this.selectAllChecked = !1, this.update();
			},
			_updateRowState(t) {
				t.selection && (t.el.dataset.state = t.selection.checked ? "checked" : "unchecked"), t.expanded.length && !t.el.dataset.expanded && (t.el.dataset.expanded = "close");
			},
			_syncSelect() {
				this.selected = this.rows.filter((t) => t.selection?.checked), this.selectedIds = this.selected.map((t) => t.id), this.selectAllChecked = it(this.rows, (t) => !!t.selection?.checked);
			}
		};
	}
	var ms = i({ textarea: () => gs });
	function gs({ maxRows: t = null, counter: e = null, length: i = 0 } = {}) {
		return {
			length: i,
			init() {
				const i = this.$el.querySelector("textarea"), s = parseInt(i.getAttribute("rows")), n = s && s > 0 && t && t > s;
				e && (this.length = i.value.length), n && this.resizeRows(i, s, t), g(i, { "@input": () => {
					e && (this.length = i.value.length), n && this.resizeRows(i, s, t);
				} });
			},
			resizeRows(t, e, i) {
				t.rows = e;
				const s = getComputedStyle(t), n = parseFloat(s.paddingTop) + parseFloat(s.paddingBottom), r = parseFloat(s.lineHeight) || 1.2 * parseFloat(s.fontSize) || 16, o = Math.round((t.scrollHeight - n) / r);
				t.rows = Math.min(Math.max(o, e), i);
			}
		};
	}
	var fs = i({ timePicker: () => ws }), vs = ["12-hour", "24-hour"], ys = 1440;
	function ws({ value: t = null, multiple: e = null, format: i = null, locale: s = null, interval: n = null, min: r = null, max: o = null, unavailable: a = null, openTo: l = null, type: h = null } = {}) {
		i && !vs.includes(i) && (console.warn(`[tallkit] tk:time-picker received an invalid "format" ("${i}"). Expected one of: ${vs.join(", ")}. Falling back to the locale default.`), i = null), n = Math.max(1, Number(n) || 30), r = r ? U(r) : null, o = o ? U(o) : null, l = l ? U(l) : null, e = Boolean(e);
		const c = Y(a).map((t) => {
			if (t.includes("-")) {
				const [e, i] = t.split("-").map((t) => U(t));
				return e && i ? [e, i] : null;
			}
			const e = U(t);
			return e ? [e, e] : null;
		}).filter(Boolean), u = $t({
			mode: "dropdown",
			position: "bottom",
			align: "start",
			matchTriggerWidth: !0
		}), d = Fe({
			key: "time-picker",
			serialize() {
				return e ? (this.value ?? []).join(",") : this.value ?? null;
			},
			deserialize(t) {
				return this.parseInitialValue(t);
			}
		});
		return {
			...u,
			...d,
			value: null,
			typed: "",
			typing: !1,
			locale: s || ("undefined" != typeof navigator ? navigator.language : "en-US"),
			init() {
				u.init.call(this), this.value = this.parseInitialValue(t), d.init.call(this), this.syncTyped(), this.$watch("value", () => this.syncTyped()), this.$watch("typed", () => {
					this.typing && this.commitTyped();
				});
			},
			onOpen() {
				u.onOpen.call(this), this.$nextTick(() => this.scrollToSelected());
			},
			parseInitialValue: (t) => e ? t ? (Array.isArray(t) ? t : Y(t)).map((t) => U(t)).filter(Boolean) : [] : t ? (Array.isArray(t) && (t = t[0]), U(t)) : null,
			slots() {
				const t = [];
				for (let e = 0; e < ys; e += n) t.push(`${D(Math.floor(e / 60))}:${D(e % 60)}`);
				return t;
			},
			isTimeDisabled: (t) => !!(r && t < r) || !!(o && t > o) || c.some(([e, i]) => t >= e && t <= i),
			isSelected(t) {
				return e ? (this.value ?? []).includes(t) : this.value === t;
			},
			select(t) {
				this.isTimeDisabled(t) || (e ? this.toggleMultiple(t) : (this.value = this.value === t ? null : t, this.close()));
			},
			toggleMultiple(t) {
				const e = this.value ?? [];
				this.value = e.includes(t) ? e.filter((e) => e !== t) : [...e, t].sort();
			},
			formatter() {
				const t = {
					hour: "numeric",
					minute: "2-digit",
					hour12: "12-hour" === i || "24-hour" !== i && void 0
				};
				try {
					return new Intl.DateTimeFormat(this.locale, t);
				} catch {
					return new Intl.DateTimeFormat(void 0, t);
				}
			},
			formatSlot(t) {
				const [e, i] = t.split(":").map(Number);
				return this.formatter().format(new Date(2e3, 0, 1, e, i));
			},
			formatted() {
				return e ? (this.value ?? []).length ? this.value.map((t) => this.formatSlot(t)).join(", ") : null : this.value ? this.formatSlot(this.value) : null;
			},
			typable: () => "input" === h && !e,
			maskPattern: () => "99:99",
			syncTyped() {
				this.typable() && (this.typed = this.value ?? "");
			},
			commitTyped() {
				if (!this.typable()) return;
				if ((this.typed.match(/\d/g) ?? []).length < 4) return;
				const t = U(this.typed);
				t && !this.isTimeDisabled(t) && (this.value = t);
			},
			confirmTyped() {
				this.commitTyped(), this.typing = !1, this.syncTyped(), this.close();
			},
			onFieldBlur(t) {
				this.$root.contains(t.relatedTarget) ? (this.commitTyped(), this.typing = !1, this.syncTyped()) : this.confirmTyped();
			},
			clear() {
				this.value = e ? [] : null, this.typed = "";
			},
			nearestSlot(t) {
				const e = P(t), i = this.slots();
				return i.reduce((t, i) => Math.abs(P(i) - e) < Math.abs(P(t) - e) ? i : t, i[0]);
			},
			scrollToSelected() {
				(this.$root.querySelector("[data-active=\"true\"]") ?? (l ? this.$root.querySelector(`[data-slot="${this.nearestSlot(l)}"]`) : null))?.scrollIntoView({ block: "nearest" });
			}
		};
	}
	var bs = i({ tinymce: () => As }), ks = {
		text: { toolbar: "bold italic underline strikethrough | removeformat" },
		heading: { toolbar: "blocks" },
		color: { toolbar: "forecolor backcolor" },
		size: { toolbar: "fontsize" },
		script: { toolbar: "subscript superscript" },
		align: { toolbar: "alignleft aligncenter alignright alignjustify" },
		link: {
			plugins: "link autolink",
			toolbar: "link"
		},
		list: {
			plugins: "lists",
			toolbar: "numlist bullist"
		},
		media: {
			plugins: "image media",
			toolbar: "image media"
		},
		table: {
			plugins: "table",
			toolbar: "table"
		},
		quote: { toolbar: "blockquote" },
		code: {
			plugins: "code codesample",
			toolbar: "code codesample"
		}
	};
	function As({ options: t = {}, scripts: e = [], mode: i = null } = {}) {
		const s = gt();
		return {
			...s,
			...pt(),
			...hi(),
			editor: null,
			init() {
				this.initField(), this.load(() => c(() => !!window.tinymce, ["https://cdn.jsdelivr.net/npm/tinymce@8/tinymce.min.js", ...e]).then(() => this.mount()));
			},
			applyExternalValue(t) {
				this.editor.setContent(t ?? "");
			},
			async mount() {
				try {
					const { plugins: e, toolbar: s } = function(t) {
						const e = li(t, ai) ?? ai;
						return e.length ? {
							plugins: e.map((t) => ks[t]?.plugins).filter(Boolean).join(" "),
							toolbar: ["undo redo", ...e.map((t) => ks[t]?.toolbar).filter(Boolean)].join(" | ")
						} : {
							plugins: "",
							toolbar: !1
						};
					}(i), [n] = await window.tinymce.init({
						target: this.input,
						license_key: "gpl",
						menubar: !1,
						plugins: e,
						toolbar: s,
						promotion: !1,
						branding: !1,
						setup: (t) => {
							t.on("change input undo redo", () => {
								this.sync(t.getContent());
							});
						},
						...t,
						...this.getDataOptions(this.input)
					});
					this.editor = n, this.$dispatch("rendered", { editor: this.editor });
				} catch (t) {
					this.fail(t);
				}
			},
			destroy() {
				s.destroy.call(this), this.editor?.remove(), this.editor = null;
			}
		};
	}
	var $s = i({ tiptap: () => Ds }), xs = "3.30.3", Es = (t, e) => `https://esm.sh/${t}@${e}`;
	function Cs() {
		const t = document.cookie.match(/(?:^|; )XSRF-TOKEN=([^;]*)/);
		return t ? decodeURIComponent(t[1]) : null;
	}
	var Ss = {
		text: {},
		heading: {},
		color: {
			scripts: (t) => [Es("@tiptap/extension-text-style", t)],
			extensions: ([t]) => [t.TextStyleKit]
		},
		size: {
			scripts: (t) => [Es("@tiptap/extension-text-style", t)],
			extensions: ([t]) => [t.TextStyleKit]
		},
		script: {
			scripts: (t) => [Es("@tiptap/extension-subscript", t), Es("@tiptap/extension-superscript", t)],
			extensions: ([t, e]) => [t.default, e.default]
		},
		align: {
			scripts: (t) => [Es("@tiptap/extension-text-align", t)],
			extensions: ([t]) => [t.default.configure({ types: ["heading", "paragraph"] })]
		},
		link: {},
		list: {},
		media: {
			scripts: (t) => [Es("@tiptap/extension-image", t), Es("@tiptap/core", t)],
			extensions: ([t, e]) => [t.default.configure({ resize: {
				enabled: !0,
				alwaysPreserveAspectRatio: !0
			} }), e.Node.create({
				name: "video",
				group: "block",
				atom: !0,
				draggable: !0,
				addAttributes: () => ({
					src: { default: null },
					width: {
						default: null,
						renderHTML: (t) => t.width ? { style: `width: ${t.width}` } : {}
					}
				}),
				parseHTML: () => [{ tag: "video" }],
				renderHTML: ({ HTMLAttributes: t }) => ["video", e.mergeAttributes({ controls: "" }, t)],
				addCommands() {
					return { setVideo: (t) => ({ commands: e }) => e.insertContent({
						type: this.name,
						attrs: t
					}) };
				}
			})]
		},
		table: {
			scripts: (t) => [Es("@tiptap/extension-table", t)],
			extensions: ([t]) => [t.TableKit]
		},
		quote: {},
		code: {}
	};
	function Ds({ options: t = {}, scripts: e = [], mode: i = null, upload: s = {}, version: n = null } = {}) {
		const r = gt();
		let o = n || xs, a = null;
		return {
			...r,
			...pt(),
			...hi(),
			groups: [],
			extraModules: [],
			tick: 0,
			init() {
				this.initField();
				const t = li(i, ai) ?? ai;
				this.groups = t, this.load(async () => {
					const [{ Editor: i }, { default: s }] = await d([Es("@tiptap/core", o), Es("@tiptap/starter-kit", o)]), n = [s];
					for (const e of t) {
						const t = Ss[e], i = t?.scripts?.(o);
						if (!i?.length) continue;
						const s = await d(i);
						for (const e of t.extensions?.(s) ?? []) e && !n.includes(e) && n.push(e);
					}
					e.length && (this.extraModules = await d(e)), this.mount(i, n);
				});
			},
			applyExternalValue(t) {
				a.commands.setContent(t ?? "");
			},
			run(t) {
				const e = a.chain().focus();
				if ("heading1" === t) e.toggleHeading({ level: 1 });
				else if ("heading2" === t) e.toggleHeading({ level: 2 });
				else if ("heading3" === t) e.toggleHeading({ level: 3 });
				else if (t.startsWith("align")) e.setTextAlign(t.slice(5).toLowerCase());
				else if ("link" === t) {
					const t = window.prompt("URL", a.getAttributes("link").href ?? "");
					t ? e.setLink({ href: t }) : e.unsetLink();
				} else "image" === t ? this.$refs.imageInput?.click() : "video" === t ? this.$refs.videoInput?.click() : "table" === t ? e.insertTable({
					rows: 3,
					cols: 3,
					withHeaderRow: !0
				}) : e[`toggle${t.charAt(0).toUpperCase()}${t.slice(1)}`]?.();
				e.run();
			},
			async handleUpload(t, e) {
				if (!s) return function(t) {
					return new Promise((e, i) => {
						const s = new FileReader();
						s.onload = () => e(s.result), s.onerror = () => i(s.error), s.readAsDataURL(t);
					});
				}(t);
				const i = s.maxSize?.[e];
				if (i && t.size > 1024 * i) throw new Error(`File is larger than the ${(i / 1024).toFixed(1)}MB limit.`);
				const n = new FormData();
				n.append("file", t), i && n.append("max_size", String(i)), s.disk && n.append("disk", s.disk), s.directory && n.append("directory", s.directory);
				const r = await fetch(s.url, {
					method: "POST",
					credentials: "same-origin",
					headers: {
						Accept: "application/json",
						"X-XSRF-TOKEN": Cs() ?? ""
					},
					body: n
				});
				if (!r.ok) throw new Error(`Upload failed with status ${r.status}`);
				return (await r.json()).url;
			},
			async insertImage(t) {
				const e = t.target, i = e.files?.[0];
				if (e.value = "", i) try {
					const t = await this.handleUpload(i, "image");
					a.chain().focus().setImage({
						src: t,
						alt: i.name
					}).run();
				} catch (t) {
					console.error(t), window.alert(t instanceof Error ? t.message : "Failed to upload image.");
				}
			},
			async insertVideo(t) {
				const e = t.target, i = e.files?.[0];
				if (e.value = "", i) try {
					const t = await this.handleUpload(i, "video");
					a.chain().focus().setVideo({ src: t }).run();
				} catch (t) {
					console.error(t), window.alert(t instanceof Error ? t.message : "Failed to upload video.");
				}
			},
			textStyle(t) {
				return this.tick, a?.getAttributes("textStyle")[t] ?? null;
			},
			isActive(t) {
				return this.tick, !!a && ("heading1" === t ? a.isActive("heading", { level: 1 }) : "heading2" === t ? a.isActive("heading", { level: 2 }) : "heading3" === t ? a.isActive("heading", { level: 3 }) : t.startsWith("align") ? a.isActive({ textAlign: t.slice(5).toLowerCase() }) : a.isActive(t));
			},
			setColor(t) {
				t ? a.chain().focus().setColor(t).run() : a.chain().focus().unsetColor().run();
			},
			setBackgroundColor(t) {
				t ? a.chain().focus().setBackgroundColor(t).run() : a.chain().focus().unsetBackgroundColor().run();
			},
			setFontSize(t) {
				t ? a.chain().focus().setFontSize(t).run() : a.chain().focus().unsetFontSize().run();
			},
			mount(e, i) {
				try {
					a = new e({
						element: this.$refs.root,
						extensions: i,
						content: this.input.value ?? "",
						editorProps: { attributes: {
							class: "tiptap-content",
							"data-tallkit-control": ""
						} },
						onUpdate: ({ editor: t }) => {
							this.sync(t.getHTML());
						},
						onSelectionUpdate: () => {
							this.tick++;
						},
						onTransaction: () => {
							this.tick++;
						},
						...t,
						...this.getDataOptions(this.$refs.root)
					}), this.$dispatch("rendered", { editor: a });
				} catch (t) {
					this.fail(t);
				}
			},
			destroy() {
				r.destroy.call(this), a?.destroy(), a = null;
			}
		};
	}
	var Ms = i({ toast: () => Ts });
	function Ts() {
		return {
			toasts: [],
			isPageVisible: !1,
			isUserActive: !1,
			idleTimeout: null,
			idleDelay: 0,
			_listeners: [],
			init() {
				g(this.$el, { "@toast.document"(t) {
					this.addToast(t.detail);
				} }), this.initAttentionListeners();
			},
			initAttentionListeners() {
				this.isPageVisible = !0, this.isUserActive = !0, this.idleTimeout = null, this.idleDelay = 1e4, this._listeners = [];
				let t = !1;
				const e = () => {
					t || (t = !0, requestAnimationFrame(() => {
						this.isUserActive = !0, this.resetIdleTimer(), this.syncAttention(), t = !1;
					}));
				}, i = () => {
					this.isUserActive = !1, this.syncAttention();
				}, s = (t, e, i, s) => {
					t.addEventListener(e, i, s), this._listeners.push(() => t.removeEventListener(e, i, s));
				};
				s(document, "visibilitychange", () => {
					this.isPageVisible = !document.hidden, this.syncAttention();
				}), [
					"mousemove",
					"mousedown",
					"keydown",
					"touchstart"
				].forEach((t) => {
					s(window, t, e, { passive: !0 });
				}), this.resetIdleTimer = () => {
					this.idleTimeout && clearTimeout(this.idleTimeout), this.idleTimeout = setTimeout(i, this.idleDelay);
				}, this.resetIdleTimer();
			},
			destroy() {
				this._listeners.forEach((t) => t()), clearTimeout(this.idleTimeout);
			},
			syncAttention() {
				const t = this.isPageVisible && this.isUserActive;
				this.toasts.forEach((e) => {
					e.duration && e.attentionAware && (t && e.pausedAt && e.resume("attention"), t || e.pausedAt || e.pause("attention"));
				});
			},
			addToast(t) {
				const e = function(t = "bottom-right") {
					return "top" === t ? "top-right" : "bottom" === t ? "bottom-right" : t;
				}(t.position), i = t.maxStack ?? 5;
				if (!1 !== i) {
					const t = this.toasts.filter((t) => t.position === e);
					if (t.length >= i) {
						const e = t.slice().sort((t, e) => t.createdAt - e.createdAt)[0];
						e && this.removeToast(e.id);
					}
				}
				const s = t.duration ?? Fs(t.title, t.message), n = this, r = t.id ? this.toasts.find((e) => e.id === t.id) : null;
				if (r) return this.updateToast(r.id, t);
				const o = window.Alpine.reactive({
					id: crypto.randomUUID?.() ?? `${Date.now()}-${Math.random().toString(36).slice(2)}`,
					createdAt: Date.now(),
					...t,
					duration: s,
					position: e,
					attentionAware: t.attentionAware ?? !0,
					progress: t.progress ?? !0,
					pauseOnHover: t.pauseOnHover ?? !0,
					swipe: t.swipe ?? !0,
					visible: !1,
					progressValue: 1,
					startTime: 0,
					total: s,
					elapsedBeforePause: 0,
					raf: null,
					pausedAt: null,
					pausedByHover: !1,
					pausedByAttention: !1,
					swiping: !1,
					startX: 0,
					startY: 0,
					currentX: 0,
					currentY: 0,
					lockDirection: null,
					start() {
						if (!this.duration) return;
						this.startTime = performance.now();
						const t = (e) => {
							if (!n.toasts.find((t) => t.id === this.id)) return void this.stop();
							if (this.pausedAt) return;
							const i = this.elapsedBeforePause + (e - this.startTime), s = Math.min(i / this.total, 1);
							this.progress && (this.progressValue = 1 - s), s >= 1 ? n.removeToast(this.id) : this.raf = requestAnimationFrame(t);
						};
						this.raf = requestAnimationFrame(t);
					},
					pause(t = "attention") {
						this.duration && ("hover" === t ? this.pausedByHover = !0 : this.pausedByAttention = !0, this.pausedAt || (this.pausedAt = performance.now(), this.elapsedBeforePause += this.pausedAt - this.startTime, this.raf && (cancelAnimationFrame(this.raf), this.raf = null)));
					},
					resume(t = "attention") {
						"hover" === t ? this.pausedByHover = !1 : this.pausedByAttention = !1, this.pausedByHover || this.pausedByAttention || this.pausedAt && (this.pausedAt = null, this.start());
					},
					stop() {
						this.raf && (cancelAnimationFrame(this.raf), this.raf = null);
					},
					onPointerDown(t) {
						this.swipe && (this.swiping = !0, this.startX = t.clientX, this.startY = t.clientY, this.lockDirection = null);
					},
					onPointerMove(t) {
						this.swipe && this.swiping && (this.currentX = t.clientX - this.startX, this.currentY = t.clientY - this.startY, this.lockDirection || (this.lockDirection = Math.abs(this.currentX) > Math.abs(this.currentY) ? "x" : "y"), "x" === this.lockDirection && t.preventDefault());
					},
					onPointerUp(t) {
						if (!this.swipe) return;
						if (this.swiping = !1, "x" !== this.lockDirection) return this.currentX = 0, this.currentY = 0, void (this.lockDirection = null);
						const e = .4 * t.currentTarget.offsetWidth;
						Math.abs(this.currentX) > e ? n.removeToast(this.id) : (this.currentX = 0, this.currentY = 0, this.lockDirection = null);
					}
				});
				return this.toasts.push(o), this.$nextTick(() => {
					o.visible = !0, o.start();
				}), o;
			},
			updateToast(t, e) {
				const i = this.toasts.find((e) => e.id === t);
				if (!i) return;
				const s = [
					"title",
					"message",
					"type",
					"size",
					"duration",
					"position",
					"attentionAware",
					"progress",
					"pauseOnHover",
					"swipe"
				];
				for (const t in e) s.includes(t) && (i[t] = e[t]);
				return i.currentX = 0, i.swiping = !1, void 0 !== e.duration && (i.stop(), i.pausedAt = null, i.pausedByHover = !1, i.pausedByAttention = !1, i.total = e.duration, i.elapsedBeforePause = 0, i.progressValue = 1, i.visible && i.start()), i;
			},
			removeToast(t) {
				const e = this.toasts.find((e) => e.id === t);
				e && (e.stop(), e.raf = null, e.visible = !1, setTimeout(() => {
					this.toasts = this.toasts.filter((e) => e.id !== t);
				}, 300));
			},
			getToastsByPosition(t) {
				return this.toasts.filter((e) => e.position === t);
			},
			notify(t) {
				return this.addToast(t);
			},
			success(t, e = {}) {
				return this.notify({
					title: t,
					type: "success",
					...e
				});
			},
			error(t, e = {}) {
				return this.notify({
					title: t,
					type: "error",
					...e
				});
			},
			info(t, e = {}) {
				return this.notify({
					title: t,
					type: "info",
					...e
				});
			},
			warning(t, e = {}) {
				return this.notify({
					title: t,
					type: "warning",
					...e
				});
			},
			loading(t, e = {}) {
				return this.notify({
					title: t,
					type: "loading",
					duration: null,
					progress: !1,
					swipe: !1,
					...e
				});
			},
			group(t, e) {
				const i = this.toasts.find((t) => t.groupKey === e);
				return i ? (i.count = (i.count || 1) + 1, i.meta = {
					...i.meta || {},
					count: i.count
				}, i.currentX = 0, i.swiping = !1, i.visible && i.duration && (i.stop(), i.pausedAt = null, i.progressValue = 1, i.start()), i) : this.addToast({
					...t,
					groupKey: e,
					count: 1,
					meta: { count: 1 }
				});
			},
			promise(t, e = {}) {
				const i = this.loading(e.loading ?? "Loading..."), s = (t, e) => "function" == typeof t ? t(e) : t;
				return t.then((t) => {
					this.toasts.find((t) => t.id === i.id) && this.updateToast(i.id, {
						title: s(e.success, t) ?? "Success!",
						type: "success",
						duration: Fs(s(e.success, t)),
						progress: !0,
						swipe: !0
					});
				}).catch((t) => {
					this.toasts.find((t) => t.id === i.id) && this.updateToast(i.id, {
						title: s(e.error, t) ?? "Error!",
						type: "error",
						duration: 1.3 * Fs(s(e.error, t)),
						progress: !0,
						swipe: !0
					});
				}), t;
			},
			queue(t, e = 3) {
				const i = this.toasts.filter((t) => t.visible);
				if (i.length >= e) {
					const t = i.slice().sort((t, e) => t.createdAt - e.createdAt)[0];
					this.removeToast(t.id);
				}
				return this.addToast(t);
			},
			dedupe(t, e = 2e3) {
				const i = Date.now();
				return this.toasts.find((s) => s.title === t.title && s.type === t.type && i - s.createdAt < e) || this.addToast({
					...t,
					createdAt: i
				});
			}
		};
	}
	function Fs(t = "", e = "") {
		const i = `${t} ${e}`.trim();
		let s = 1e3 + (1.2 * t.length + 1.6 * e.length) / 16 * 1e3;
		return s += 300 * i.split("\n").length, Math.min(9e3, Math.max(3e3, s));
	}
	var Is = i({ toggleAll: () => _s });
	function _s({ group: t = null } = {}) {
		return Oe("toggle", t ?? "");
	}
	var Ls = i({ upload: () => Os }), Bs = [
		"image",
		"video",
		"audio",
		"pdf"
	];
	function Os({ wireModel: t = !1, multiple: e = !1, droppable: i = !0, maxSize: s = null, maxFiles: n = null, sortable: r = !1, invalid: o = !1, files: a = [], tooLargeMessage: l = "This file is too large.", invalidTypeMessage: h = "This file type is not allowed.", tooManyFilesMessage: c = "Too many files selected.", previewName: u = null } = {}) {
		return {
			dragOver: !1,
			dragIndex: null,
			dragOverIndex: null,
			sortable: r,
			previewId: null,
			files: a.map((t) => ({
				id: t.id ?? J("upload-file"),
				raw: null,
				name: t.name ?? "",
				size: t.size ?? 0,
				url: t.url ?? null,
				value: t.value ?? null,
				type: t.type ?? "unknown",
				status: t.status ?? "done",
				progress: t.progress ?? 100,
				error: null,
				tmpFilename: t.tmpFilename ?? null,
				previewLoaded: Bs.includes(t.type ?? "")
			})),
			queue: [],
			activeId: null,
			multiple() {
				return this.$refs.fileInput?.multiple ?? e;
			},
			accept() {
				return this.$refs.fileInput?.accept || null;
			},
			activeFiles() {
				return this.files.filter((t) => "uploading" === t.status || "queued" === t.status);
			},
			hasPendingUploads() {
				return this.files.some((t) => "uploading" === t.status || "queued" === t.status);
			},
			aggregateProgress() {
				const t = this.activeFiles();
				return t.length ? Math.round(t.reduce((t, e) => t + e.progress, 0) / t.length) : 100;
			},
			isUploading() {
				return this.activeFiles().length > 0;
			},
			hasError() {
				return this.files.some((t) => "error" === t.status);
			},
			isInvalid() {
				return o || this.hasError();
			},
			previewFile() {
				return this.find(this.previewId);
			},
			init() {
				g(this.$refs.fileInput, { "@change"(t) {
					const e = t.target;
					this.addFiles(e.files), e.value = "";
				} }), i && g(this.$root.querySelector(K("upload-dropzone")), {
					"@dragover.prevent"() {
						this.dragOver = !0;
					},
					"@dragleave.prevent"(t) {
						t.currentTarget && t.currentTarget.contains(t.relatedTarget) || (this.dragOver = !1);
					},
					"@drop.prevent"(t) {
						this.dragOver = !1, this.addFiles(t.dataTransfer?.files ?? null);
					}
				});
			},
			destroy() {
				this.files.forEach((t) => this.revoke(t));
			},
			selectFile() {
				this.$refs.fileInput.click();
			},
			viewFile(t) {
				this.previewId = t, this.previewFile() ? this.previewFile().previewLoaded ? this.$dispatch("modal-show", { name: u }) : this.openFile() : this.previewId = null;
			},
			openFile() {
				const t = this.previewFile()?.url;
				t && window.open(t, "_blank", "noopener");
			},
			addFiles(t) {
				if (!t?.length) return;
				this.multiple() || (this.activeId && this.cancelUpload(this.activeId), this.files.forEach((t) => this.revoke(t)), this.files = [], this.queue = []);
				const e = Array.from(t), i = this.multiple() ? n ? Math.max(n - this.files.length, 0) : 1 / 0 : 1, s = e.slice(0, i), r = this.multiple() && n ? e.slice(i) : [];
				s.forEach((t) => {
					const e = this.createFileEntry(t);
					this.files.push(e), e.error || this.queue.push(e.id);
				}), r.forEach((t) => {
					const e = {
						id: J("upload-file"),
						raw: t,
						name: t.name,
						size: t.size,
						url: null,
						value: null,
						type: V(t.type, t.name),
						status: "error",
						progress: 0,
						error: c,
						tmpFilename: null
					};
					this.files.push(e);
				}), this.processQueue(), this.syncFieldError();
			},
			createFileEntry(t) {
				const e = V(t.type, t.name), i = Bs.includes(e), s = i ? URL.createObjectURL(t) : null, n = this.validate(t);
				return {
					id: J("upload-file"),
					raw: t,
					name: t.name,
					size: t.size,
					url: s,
					value: null,
					type: e,
					status: n ? "error" : "queued",
					progress: 0,
					error: n,
					tmpFilename: null,
					previewLoaded: i
				};
			},
			validate(t) {
				return s && t.size > 1024 * s ? l : this.accept() && !this.matchesAccept(t, this.accept()) ? h : null;
			},
			matchesAccept: (t, e) => e.split(",").some((e) => !!(e = e.trim()) && (e.startsWith(".") ? t.name.toLowerCase().endsWith(e.toLowerCase()) : e.endsWith("/*") ? t.type.startsWith(e.slice(0, -1)) : t.type === e)),
			processQueue() {
				if (this.activeId || !this.queue.length) return;
				const e = this.find(this.queue.shift());
				if (e) {
					if (this.activeId = e.id, e.status = "uploading", !this.$wire || !t) return e.status = "done", e.progress = 100, this.activeId = null, void this.$nextTick(() => this.processQueue());
					this.$wire.upload(t, e.raw, (t) => {
						e.status = "done", e.progress = 100, e.tmpFilename = t, this.activeId = null, this.processQueue(), this.syncFieldError();
					}, (t) => {
						e.status = "error", e.error = t || "Upload failed.", this.activeId = null, this.processQueue(), this.syncFieldError();
					}, (t) => {
						e.progress = t.detail.progress;
					}, () => {
						e.status = "cancelled", this.activeId = null, this.processQueue();
					});
				} else this.processQueue();
			},
			retryUpload(t) {
				const e = this.find(t);
				e?.raw && (e.status = "queued", e.error = null, e.progress = 0, this.queue.unshift(e.id), this.processQueue());
			},
			cancelUpload(e) {
				e === this.activeId && this.$wire && t && this.$wire.cancelUpload(t);
			},
			removeFile(t) {
				const e = this.files.findIndex((e) => e.id === t);
				if (-1 === e) return;
				const i = this.files[e];
				i.id === this.activeId ? this.cancelUpload(t) : this.queue = this.queue.filter((e) => e !== t), this.detachFromWire(i, this.files.filter((e) => e.id !== t)), this.revoke(i), this.files.splice(e, 1), this.syncFieldError();
			},
			replaceFile(t, e) {
				const i = e?.[0], s = this.files[t];
				if (!i || !s) return;
				s.id === this.activeId ? this.cancelUpload(s.id) : this.queue = this.queue.filter((t) => t !== s.id), this.detachFromWire(s, this.files.filter((t) => t.id !== s.id)), this.revoke(s);
				const n = this.createFileEntry(i);
				this.files.splice(t, 1, n), n.error || (this.queue.push(n.id), this.processQueue()), this.syncFieldError();
			},
			detachFromWire(e, i) {
				this.$wire && t && (e.tmpFilename ? this.$wire.removeUpload(t, e.tmpFilename) : null !== e.value && (this.multiple() ? this.hasPendingUploads() || this.$wire.set(t, i.filter((t) => null !== t.value).map((t) => t.value)) : this.$wire.set(t, null)));
			},
			syncFieldError() {
				this.isInvalid() || tt(this.$root, "error")?.remove();
			},
			revoke(t) {
				t.raw && t.url && URL.revokeObjectURL(t.url);
			},
			find(t) {
				return this.files.find((e) => e.id === t) ?? null;
			},
			dragStart(t, e) {
				this.dragIndex = t, e.dataTransfer?.setData("text/plain", String(t));
			},
			dragOverTile(t) {
				this.dragIndex !== t && (this.dragOverIndex = t);
			},
			dragLeaveTile(t, e) {
				this.dragOverIndex === t && (e.currentTarget && e.currentTarget.contains(e.relatedTarget) || (this.dragOverIndex = null));
			},
			dropOnTile(t, e) {
				this.dragOverIndex = null, this.dragOver = !1;
				const i = e.dataTransfer?.files;
				i?.length ? this.replaceFile(t, i) : this.drop(t);
			},
			drop(e) {
				if (null === this.dragIndex || this.dragIndex === e) return;
				const [i] = this.files.splice(this.dragIndex, 1);
				this.files.splice(e, 0, i), this.dragIndex = null, this.multiple() && this.$wire && t && !this.hasPendingUploads() && this.$wire.set(t, this.files.filter((t) => null !== t.value).map((t) => t.value));
			},
			dragEnd() {
				this.dragIndex = null, this.dragOverIndex = null;
			},
			formatSize: (t) => function(t, e = 1) {
				if (!t) return "0 B";
				const i = [
					"B",
					"KB",
					"MB",
					"GB",
					"TB"
				], s = Math.min(Math.floor(Math.log(t) / Math.log(1024)), i.length - 1), n = t / Math.pow(1024, s);
				return `${0 === s ? n : n.toFixed(e)} ${i[s]}`;
			}(t)
		};
	}
	async function js() {
		window.Alpine || await h([
			"https://unpkg.com/@alpinejs/resize@3.x.x/dist/cdn.min.js",
			"https://unpkg.com/@alpinejs/mask@3.x.x/dist/cdn.min.js",
			"https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js"
		]);
	}
	function zs(t) {
		const e = window.Alpine;
		e && (function() {
			const t = Object.fromEntries(Object.values([
				lt,
				ut,
				ft,
				wt,
				Se,
				Me,
				Ie,
				Le,
				je,
				qe,
				Re,
				Ue,
				He,
				Ye,
				Xe,
				Qe,
				ei,
				si,
				ri,
				ci,
				pi,
				gi,
				vi,
				wi,
				ki,
				$i,
				Ei,
				Si,
				Ee,
				mt,
				Ti,
				Ii,
				Li,
				Oi,
				zi,
				Ni,
				Pi,
				Wi,
				Vi,
				At,
				Ji,
				Zi,
				ts,
				ns,
				os,
				ls,
				cs,
				ds,
				ms,
				fs,
				bs,
				$s,
				Ms,
				Is,
				Ls
			]).flatMap((t) => Object.entries(t).filter(([, t]) => "function" == typeof t)));
			for (const [e, i] of Object.entries(t)) window.Alpine.data(e, i);
		}(), e.store("tallkit", t), e.magic("tallkit", () => t), e.magic("tk", () => t));
	}
	var qs = {
		mode: window.localStorage.getItem("tallkit.appearance") || "system",
		init() {
			this.apply(this.mode), document.addEventListener("livewire:navigated", () => this.apply(this.mode)), window.matchMedia("(prefers-color-scheme: dark)").addEventListener("change", () => {
				"system" === this.mode && this.apply("system");
			});
		},
		isDark: () => document.documentElement.classList.contains("dark"),
		isLight() {
			return !this.isDark();
		},
		applyDark(t = !0) {
			document.documentElement.classList.add("dark"), t && window.localStorage.setItem("tallkit.appearance", "dark"), this.mode = "dark";
		},
		applyLight(t = !0) {
			document.documentElement.classList.remove("dark"), t && window.localStorage.setItem("tallkit.appearance", "light"), this.mode = "light";
		},
		apply(t) {
			if ("system" === t) {
				const t = window.matchMedia("(prefers-color-scheme: dark)");
				window.localStorage.removeItem("tallkit.appearance"), t.matches ? this.applyDark(!1) : this.applyLight(!1), this.mode = "system";
			} else "dark" === t ? this.applyDark() : "light" === t && this.applyLight();
		},
		toggle(t, e = {}) {
			if ("undefined" == typeof document || "function" != typeof document.startViewTransition || window.matchMedia("(prefers-reduced-motion: reduce)").matches || !t) return this.isDark() ? this.applyLight() : this.applyDark();
			const i = document.startViewTransition(() => this.isDark() ? this.applyLight() : this.applyDark()), s = t.clientX || 0, n = t.clientY || 0, r = Math.hypot(Math.max(s, innerWidth - s), Math.max(n, innerHeight - n));
			i.ready.then(() => {
				const t = [`circle(0px at ${s}px ${n}px)`, `circle(${r}px at ${s}px ${n}px)`];
				document.documentElement.animate({ clipPath: this.isDark() ? [...t].reverse() : t }, {
					duration: 300,
					easing: "ease-in",
					...e || {},
					pseudoElement: this.isDark() ? "::view-transition-old(root)" : "::view-transition-new(root)"
				});
			});
		}
	};
	var Ns = (...t) => {
		if ("object" == typeof t[0] && null !== t[0] && !Array.isArray(t[0])) return t[0];
		const [e, i, s, n, r, o, a] = t;
		return {
			message: e,
			title: i,
			type: s,
			duration: n,
			position: r,
			progress: o,
			size: a
		};
	}, Rs = {
		appearance: qs,
		toast: function t(...e) {
			if (0 === e.length) return {
				success: (...e) => t({
					...Ns(...e),
					type: "success"
				}),
				error: (...e) => t({
					...Ns(...e),
					type: "error"
				}),
				info: (...e) => t({
					...Ns(...e),
					type: "info"
				}),
				warning: (...e) => t({
					...Ns(...e),
					type: "warning"
				})
			};
			document.dispatchEvent(new CustomEvent("toast", { detail: Ns(...e) }));
		},
		loadScript: h,
		loadStyle: m,
		modal: (t) => ({
			show: () => {
				document.dispatchEvent(new CustomEvent("modal-show", { detail: { name: t } }));
			},
			close: () => {
				document.dispatchEvent(new CustomEvent("modal-close", { detail: { name: t } }));
			}
		}),
		modals: () => ({ close: () => {
			document.dispatchEvent(new CustomEvent("modal-close", { detail: {} }));
		} })
	};
	window.TALLKit = window.TK = window.tk = window.tallkit = Rs, document.dispatchEvent(new CustomEvent("tallkit:init")), "loading" === document.readyState ? document.addEventListener("DOMContentLoaded", js) : js(), document.addEventListener("alpine:init", () => zs(Rs)), t.tallkit = Rs;
});
