{
    "type": "module",
    "scripts": {
        "lint": "echo;",
        "build": "vite build",
        "watch": "vite build --watch"
    },
    "devDependencies": {
        "@rollup/plugin-terser": "^1.0",
        "vite": "^8.2"
    },
    "dependencies": {
        "fuse.js": "^7.5"
    }
}
